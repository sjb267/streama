# 微服务接口文档

## 1. 文档说明
- 更新时间：2026-05-27。
- 文档基于当前 streama-cloud 代码仓整理，采用“代码优先”口径。
- HTTP 接口明细仅描述网关对外路径，不包含内部调用接口（如 /innerApi/**）。
- 本文档同步补充 AI 审核异步消息契约，不新增/删除运行时 API。
- 当前对外 HTTP 接口总数：65（/web 29、/interact 13、/admin 16、/file 7）。

## 2. 网关与服务路由
- 网关统一入口：http://{host}:7071
- 路由规则（StripPrefix=1）：

| 网关前缀 | 目标服务 | 默认端口 | 备注 |
| --- | --- | --- | --- |
| /web/** | streama-cloud-web | 7072 | Web 业务接口 |
| /interact/** | streama-cloud-interact | 7073 | 互动业务接口 |
| /admin/** | streama-cloud-admin | 7070 | 管理端业务接口 |
| /file/** | streama-cloud-resource | 7074 | 资源与文件接口 |

> 说明：AdminFilter 在网关侧仍为未闭环实现，文档中的“登录要求”以 Controller 上 @GlobalInterceptor(checkLogin=true) 为准。

## 3. 公共约定
### 3.1 响应结构（ResponseVO）
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```

### 3.2 常见业务码
| code | 含义 |
| --- | --- |
| 200 | 请求成功 |
| 404 | 路径不存在/资源不存在 |
| 500 | 服务内部异常 |
| 600 | 参数错误/业务校验失败 |
| 601 | 数据已存在 |
| 901 | 未登录或登录过期 |

### 3.3 登录态与 Token
- Web/Interact/Resource 侧登录态：token。
- Admin 侧登录态：adminToken。
- 登录接口会写入 Cookie；业务请求通常通过 Header 读取 token。
- “登录要求（代码声明）”字段仅反映注解声明，不等价于网关层已经统一强校验。

### 3.4 参数来源规则
- 普通参数来源：Controller 方法签名与校验注解（@NotNull、@NotEmpty、@Size、@PathVariable）。
- Query 对象展开：CategoryInfoQuery、VideoInfoPostQuery、BaseQuery。
- 文件上传接口统一使用 multipart/form-data。

## 4. 枚举与参数字典
### 4.1 用户行为 actionType（/interact/userAction/doAction）
| 值 | 含义 |
| --- | --- |
| 0 | 评论点赞 |
| 1 | 评论点踩 |
| 2 | 视频点赞 |
| 3 | 视频收藏 |
| 4 | 视频投币 |
| 5 | 视频评论计数（内部） |
| 6 | 视频弹幕计数（内部） |
| 7 | 视频播放计数（内部） |

### 4.2 视频状态 status
| 值 | 含义 |
| --- | --- |
| 0 | 转码中 |
| 1 | 转码失败 |
| 2 | 待审核 |
| 3 | 审核通过 |
| 4 | 审核不通过 |

### 4.3 视频排序 orderType（/web/home/loadVideoList）
| 值 | 含义 |
| --- | --- |
| 0 | 按发布时间（默认） |
| 1 | 按播放数 |
| 2 | 按收藏数 |

### 4.4 搜索排序 orderType（/web/video/search）
| 值 | 含义 |
| --- | --- |
| 0 | 按播放量 |
| 1 | 按时间 |
| 2 | 按弹幕量 |
| 3 | 按收藏量 |

### 4.5 投稿类型 postType
| 值 | 含义 |
| --- | --- |
| 0 | 自制 |
| 1 | 转载 |

### 4.6 互动位 interaction
- 字符串包含 0：关闭评论区。
- 字符串包含 1：关闭弹幕。

### 4.7 注册密码规则
- 参数：registerPassword
- 正则：^(?=.*\d)(?=.*[a-zA-Z])[\da-zA-Z~!@#$%^&*_]{8,18}$
- 含义：8-18 位，必须包含数字与字母，可含 ~!@#$%^&*_。

### 4.8 AI 审核来源 sourceType
| 值 | 含义 |
| --- | --- |
| 1 | 新增投稿 |
| 2 | 编辑重审 |

### 4.9 AI 任务状态 taskStatus
| 值 | 含义 |
| --- | --- |
| 0 | 待处理 |
| 1 | 处理中 |
| 2 | 完成 |
| 3 | 失败 |
| 4 | 取消 |

### 4.10 AI 分 P 状态 itemStatus
| 值 | 含义 |
| --- | --- |
| 0 | 待处理 |
| 1 | 处理中 |
| 2 | 完成 |
| 3 | 失败 |

### 4.11 AI 审核建议 aiDecision / itemDecision / videoDecision
| 值 | 含义 |
| --- | --- |
| 1 | 建议通过 |
| 2 | 建议驳回 |
| 3 | 建议人工复核 |

### 4.12 AI 风险等级 aiRiskLevel / videoRiskLevel
| 值 | 含义 |
| --- | --- |
| 1 | 低风险 |
| 2 | 中风险 |
| 3 | 高风险 |

## 5. 完整接口明细

### 5.1 Web 服务（/web）
- 接口数量：29

#### 5.1.1 `/web/account/autoLogin`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：autoLogin
- 请求参数：无

- 请求示例：
```http
GET /web/account/autoLogin
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.2 `/web/account/checkCode`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：checkCode
- 请求参数：无

- 请求示例：
```http
POST /web/account/checkCode
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "checkCode": "data:image/jpeg;base64,...",
    "checkCodeKey": "uuid"
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.3 `/web/account/getUserCountInfo`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：getUserCountInfo
- 请求参数：无

- 请求示例：
```http
GET /web/account/getUserCountInfo
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.4 `/web/account/login`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：login
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| email | String | 是 | Query/Form | NotEmpty + Email | - |
| password | String | 是 | Query/Form | NotEmpty | - |
| checkCodeKey | String | 是 | Query/Form | NotEmpty | - |
| checkCode | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
POST /web/account/login
Content-Type: application/x-www-form-urlencoded

email=<value>
password=<value>
checkCodeKey=<value>
checkCode=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.5 `/web/account/logout`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：logout
- 请求参数：无

- 请求示例：
```http
POST /web/account/logout
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.6 `/web/account/register`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：register
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| email | String | 是 | Query/Form | NotEmpty + Email + Size(max = 150) | - |
| nickname | String | 是 | Query/Form | NotEmpty + Size(max = 20) | - |
| registerPassword | String | 是 | Query/Form | NotEmpty + Pattern(regexp = Constants.REGEX_PASSWORD) | - |
| checkCodeKey | String | 是 | Query/Form | NotEmpty | - |
| checkCode | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
POST /web/account/register
Content-Type: application/x-www-form-urlencoded

email=<value>
nickname=<value>
registerPassword=<value>
checkCodeKey=<value>
checkCode=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.7 `/web/category/loadAllCategory`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：loadCategory
- 请求参数：无

- 请求示例：
```http
GET /web/category/loadAllCategory
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.8 `/web/home/cancelFocus`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：cancelFocus
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| focusUserId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
POST /web/home/cancelFocus
Content-Type: application/x-www-form-urlencoded

focusUserId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.9 `/web/home/focus`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：focus
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| focusUserId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
POST /web/home/focus
Content-Type: application/x-www-form-urlencoded

focusUserId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.10 `/web/home/getUserInfo`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：getUserInfo
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| userId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
GET /web/home/getUserInfo?userId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.11 `/web/home/loadFansList`
- 建议方法：GET
- 登录要求（代码声明）：是
- 说明：loadFansList
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| pageNo | Integer | 否 | Query/Form | - | - |

- 请求示例：
```http
GET /web/home/loadFansList?pageNo=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.12 `/web/home/loadFocusList`
- 建议方法：GET
- 登录要求（代码声明）：是
- 说明：loadFocusList
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| pageNo | Integer | 否 | Query/Form | - | - |

- 请求示例：
```http
GET /web/home/loadFocusList?pageNo=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.13 `/web/home/loadVideoList`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：loadVideoList
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| userId | String | 是 | Query/Form | NotEmpty | - |
| type | Integer | 否 | Query/Form | - | - |
| pageNo | Integer | 否 | Query/Form | - | - |
| videoName | String | 否 | Query/Form | - | - |
| orderType | Integer | 否 | Query/Form | - | - |

- 请求示例：
```http
GET /web/home/loadVideoList?userId=<value>&type=<value>&pageNo=<value>&videoName=<value>&orderType=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.14 `/web/home/updateUserInfo`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：updateUserInfo
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| nickName | String | 是 | Query/Form | NotEmpty + Size(max = 20) | - |
| avatar | String | 是 | Query/Form | NotEmpty + Size(max = 100) | - |
| sex | Integer | 是 | Query/Form | NotNull | - |
| birthday | String | 是 | Query/Form | NotEmpty + Size(max = 10) | - |
| school | String | 是 | Query/Form | NotEmpty + Size(max = 150) | - |
| personIntroduction | String | 是 | Query/Form | NotEmpty + Size(max = 80) | - |
| noticeInfo | String | 否 | Query/Form | Size(max = 300) | - |

- 请求示例：
```http
POST /web/home/updateUserInfo
Content-Type: application/x-www-form-urlencoded

nickName=<value>
avatar=<value>
sex=<value>
birthday=<value>
school=<value>
personIntroduction=<value>
noticeInfo=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.15 `/web/sysSetting/getSetting`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：getSetting
- 请求参数：无

- 请求示例：
```http
GET /web/sysSetting/getSetting
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.16 `/web/ucenter/deleteVideo`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：deleteVideo
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
POST /web/ucenter/deleteVideo
Content-Type: application/x-www-form-urlencoded

videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.17 `/web/ucenter/getVideoByVideoId`
- 建议方法：GET
- 登录要求（代码声明）：是
- 说明：getVideoByVideoId
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
GET /web/ucenter/getVideoByVideoId?videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.18 `/web/ucenter/getVideoCountInfo`
- 建议方法：GET
- 登录要求（代码声明）：是
- 说明：getVideoCountInfo
- 请求参数：无

- 请求示例：
```http
GET /web/ucenter/getVideoCountInfo
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.19 `/web/ucenter/loadAllVideo`
- 建议方法：GET
- 登录要求（代码声明）：是
- 说明：loadAllVideo
- 请求参数：无

- 请求示例：
```http
GET /web/ucenter/loadAllVideo
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.20 `/web/ucenter/loadVideoPostList`
- 建议方法：GET
- 登录要求（代码声明）：是
- 说明：loadVideoPost
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| status | Integer | 否 | Query/Form | - | - |
| pageNo | Integer | 否 | Query/Form | - | - |
| videoNameFuzzy | String | 否 | Query/Form | - | - |

- 请求示例：
```http
GET /web/ucenter/loadVideoPostList?status=<value>&pageNo=<value>&videoNameFuzzy=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.21 `/web/ucenter/postVideo`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：postVideo
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 否 | Query/Form | - | - |
| videoCover | String | 是 | Query/Form | NotEmpty | - |
| videoName | String | 是 | Query/Form | NotEmpty + Size(max = 100) | - |
| pCategoryId | Integer | 是 | Query/Form | NotNull | - |
| categoryId | Integer | 否 | Query/Form | - | - |
| postType | Integer | 是 | Query/Form | NotNull | - |
| tags | String | 是 | Query/Form | NotEmpty + Size(max = 300) | - |
| introduction | String | 否 | Query/Form | Size(max = 2000) | - |
| interaction | String | 否 | Query/Form | Size(max = 3) | - |
| uploadFileList | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
POST /web/ucenter/postVideo
Content-Type: application/x-www-form-urlencoded

videoId=<value>
videoCover=<value>
videoName=<value>
pCategoryId=<value>
categoryId=<value>
postType=<value>
tags=<value>
introduction=<value>
interaction=<value>
uploadFileList=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.22 `/web/ucenter/saveVideoInteraction`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：saveVideoInteraction
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | - |
| interaction | String | 否 | Query/Form | - | - |

- 请求示例：
```http
POST /web/ucenter/saveVideoInteraction
Content-Type: application/x-www-form-urlencoded

videoId=<value>
interaction=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.1.23 `/web/video/getSearchKeywordTop`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：getSearchKeywordTop
- 请求参数：无

- 请求示例：
```http
GET /web/video/getSearchKeywordTop
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.24 `/web/video/getVideoInfo`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：getVideoInfo
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
GET /web/video/getVideoInfo?videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.25 `/web/video/getVideoRecommend`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：getVideoRecommend
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| keyword | String | 是 | Query/Form | NotEmpty | - |
| videoId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
GET /web/video/getVideoRecommend?keyword=<value>&videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.26 `/web/video/loadRecommendVideoList`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：loadRecommendVideo
- 请求参数：无

- 请求示例：
```http
GET /web/video/loadRecommendVideoList
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.27 `/web/video/loadVideoList`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：loadVideoList
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| pCategoryId | Integer | 否 | Query/Form | - | - |
| categoryId | Integer | 否 | Query/Form | - | - |
| pageNo | Integer | 否 | Query/Form | - | - |
| pageSize | Integer | 否 | Query/Form | - | - |

- 请求示例：
```http
GET /web/video/loadVideoList?pCategoryId=<value>&categoryId=<value>&pageNo=<value>&pageSize=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.28 `/web/video/loadVideoPList`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：loadVideoPList
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
GET /web/video/loadVideoPList?videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.1.29 `/web/video/search`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：search
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| keyword | String | 是 | Query/Form | NotEmpty | - |
| orderType | Integer | 是 | Query/Form | NotNull | - |
| pageNo | Integer | 否 | Query/Form | - | - |

- 请求示例：
```http
GET /web/video/search?keyword=<value>&orderType=<value>&pageNo=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

### 5.2 Interact 服务（/interact）
- 接口数量：13

#### 5.2.1 `/interact/comment/cancelTopComment`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：cancelTopComment
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| commentId | Integer | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
POST /interact/comment/cancelTopComment
Content-Type: application/x-www-form-urlencoded

commentId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.2.2 `/interact/comment/loadComment`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：loadComment
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | - |
| pageNo | Integer | 否 | Query/Form | - | - |
| orderType | Integer | 否 | Query/Form | - | - |

- 请求示例：
```http
GET /interact/comment/loadComment?videoId=<value>&pageNo=<value>&orderType=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.2.3 `/interact/comment/postComment`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：postComment
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | - |
| content | String | 是 | Query/Form | NotEmpty + Size(max=500) | - |
| replyCommentId | Integer | 否 | Query/Form | - | - |
| imgPath | String | 否 | Query/Form | Size(max = 50) | - |

- 请求示例：
```http
POST /interact/comment/postComment
Content-Type: application/x-www-form-urlencoded

videoId=<value>
content=<value>
replyCommentId=<value>
imgPath=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.2.4 `/interact/comment/topComment`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：topComment
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| commentId | Integer | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
POST /interact/comment/topComment
Content-Type: application/x-www-form-urlencoded

commentId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.2.5 `/interact/comment/userDelComment`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：userDelComment
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| commentId | Integer | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
POST /interact/comment/userDelComment
Content-Type: application/x-www-form-urlencoded

commentId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.2.6 `/interact/danmu/loadDanmu`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：loadDanmu
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| fileId | String | 是 | Query/Form | NotEmpty | - |
| videoId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
GET /interact/danmu/loadDanmu?fileId=<value>&videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.2.7 `/interact/danmu/postDanmu`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：postDanmu
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | - |
| fileId | String | 是 | Query/Form | NotEmpty | - |
| text | String | 是 | Query/Form | NotEmpty + Size(max = 200) | - |
| mode | Integer | 是 | Query/Form | NotNull | - |
| color | String | 是 | Query/Form | NotEmpty | - |
| time | Integer | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
POST /interact/danmu/postDanmu
Content-Type: application/x-www-form-urlencoded

videoId=<value>
fileId=<value>
text=<value>
mode=<value>
color=<value>
time=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.2.8 `/interact/home/loadUserCollection`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：loadUserCollection
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| userId | String | 是 | Query/Form | NotEmpty | - |
| pageNo | Integer | 否 | Query/Form | - | - |

- 请求示例：
```http
GET /interact/home/loadUserCollection?userId=<value>&pageNo=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.2.9 `/interact/ucenter/delComment`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：delComment
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| commentId | Integer | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
POST /interact/ucenter/delComment
Content-Type: application/x-www-form-urlencoded

commentId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.2.10 `/interact/ucenter/delDanmu`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：delDanmu
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| danmuId | Integer | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
POST /interact/ucenter/delDanmu
Content-Type: application/x-www-form-urlencoded

danmuId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.2.11 `/interact/ucenter/loadComment`
- 建议方法：GET
- 登录要求（代码声明）：是
- 说明：loadComment
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| pageNo | Integer | 否 | Query/Form | - | - |
| videoId | String | 否 | Query/Form | - | - |

- 请求示例：
```http
GET /interact/ucenter/loadComment?pageNo=<value>&videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.2.12 `/interact/ucenter/loadDanmu`
- 建议方法：GET
- 登录要求（代码声明）：是
- 说明：loadDanmu
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| pageNo | Integer | 否 | Query/Form | - | - |
| videoId | String | 否 | Query/Form | - | - |

- 请求示例：
```http
GET /interact/ucenter/loadDanmu?pageNo=<value>&videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.2.13 `/interact/userAction/doAction`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：doAction
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | - |
| actionType | Integer | 是 | Query/Form | NotNull | - |
| actionCount | Integer | 否 | Query/Form | - | - |
| commentId | Integer | 否 | Query/Form | - | - |

- 请求示例：
```http
POST /interact/userAction/doAction
Content-Type: application/x-www-form-urlencoded

videoId=<value>
actionType=<value>
actionCount=<value>
commentId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

### 5.3 Resource 服务（/file）
- 接口数量：7

#### 5.3.1 `/file/deleteUploadVideo`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：deleteUploadVideo
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| uploadId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
POST /file/deleteUploadVideo
Content-Type: application/x-www-form-urlencoded

uploadId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.3.2 `/file/getResource`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：getResource
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| sourceName | String | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
GET /file/getResource?sourceName=<value>
```

- 成功响应示例：
（二进制流响应，非 JSON）
- 资源读取接口直接输出文件字节流。
- 成功时通常返回 200，并由 Content-Type 表示资源类型。
- 常见错误码：200 / 600 / 500

#### 5.3.3 `/file/preUploadVideo`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：preUploadVideo
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| fileName | String | 是 | Query/Form | NotEmpty | - |
| chunks | Integer | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
POST /file/preUploadVideo
Content-Type: application/x-www-form-urlencoded

fileName=<value>
chunks=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.3.4 `/file/uploadImage`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：uploadImage
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| file | MultipartFile | 是 | FormData | NotNull | - |
| createThumbnail | Boolean | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
POST /file/uploadImage
Content-Type: multipart/form-data

file=<binary>
createThumbnail=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.3.5 `/file/uploadVideo`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：uploadVideo
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| chunkFile | MultipartFile | 是 | FormData | NotNull | - |
| chunkIndex | Integer | 是 | Query/Form | NotNull | - |
| uploadId | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
POST /file/uploadVideo
Content-Type: multipart/form-data

chunkFile=<binary>
chunkIndex=<value>
uploadId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.3.6 `/file/videoResource/{fileId}`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：videoResource
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| fileId | String | 是 | Path | PathVariable("fileId") + NotEmpty | - |

- 请求示例：
```http
GET /file/videoResource/{fileId}
```

- 成功响应示例：
（二进制流响应，非 JSON）
- 资源读取接口直接输出文件字节流。
- 成功时通常返回 200，并由 Content-Type 表示资源类型。
- 常见错误码：200 / 600 / 500

#### 5.3.7 `/file/videoResource/{fileId}/{ts}`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：videoResourceTs
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| fileId | String | 是 | Path | PathVariable("fileId") + NotEmpty | - |
| ts | String | 是 | Path | PathVariable("ts") + NotEmpty | - |

- 请求示例：
```http
GET /file/videoResource/{fileId}/{ts}
```

- 成功响应示例：
（二进制流响应，非 JSON）
- 资源读取接口直接输出文件字节流。
- 成功时通常返回 200，并由 Content-Type 表示资源类型。
- 常见错误码：200 / 600 / 500

### 5.4 Admin 服务（/admin）
- 接口数量：16

#### 5.4.1 `/admin/account/checkCode`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：checkCode
- 请求参数：无

- 请求示例：
```http
POST /admin/account/checkCode
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "checkCode": "data:image/jpeg;base64,...",
    "checkCodeKey": "uuid"
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.2 `/admin/account/login`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：login
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| account | String | 是 | Query/Form | NotEmpty | - |
| password | String | 是 | Query/Form | NotEmpty | - |
| checkCodeKey | String | 是 | Query/Form | NotEmpty | - |
| checkCode | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
POST /admin/account/login
Content-Type: application/x-www-form-urlencoded

account=<value>
password=<value>
checkCodeKey=<value>
checkCode=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.3 `/admin/account/logout`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：logout
- 请求参数：无

- 请求示例：
```http
POST /admin/account/logout
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.4 `/admin/category/changeSort`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：changeSort
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| pCategoryId | Integer | 是 | Query/Form | NotNull | - |
| categoryIds | String | 是 | Query/Form | NotEmpty | - |

- 请求示例：
```http
POST /admin/category/changeSort
Content-Type: application/x-www-form-urlencoded

pCategoryId=<value>
categoryIds=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {}
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.5 `/admin/category/delCategory`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：delCategory
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| categoryId | Integer | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
POST /admin/category/delCategory
Content-Type: application/x-www-form-urlencoded

categoryId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.6 `/admin/category/loadCategory`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：loadCategory
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| categoryId | Integer | 否 | Query | - | 分类ID |
| categoryCode | String | 否 | Query | - | 分类编码 |
| categoryCodeFuzzy | String | 否 | Query | - | 分类编码模糊匹配 |
| categoryName | String | 否 | Query | - | 分类名称 |
| categoryNameFuzzy | String | 否 | Query | - | 分类名称模糊匹配 |
| pCategoryId | Integer | 否 | Query | - | 父分类ID |
| icon | String | 否 | Query | - | 图标 |
| iconFuzzy | String | 否 | Query | - | 图标模糊匹配 |
| sort | Integer | 否 | Query | - | 排序号 |
| categoryIdOrPCategoryId | Integer | 否 | Query | - | 分类ID或父分类ID |
| pageNo | Integer | 否 | Query | - | 页码（BaseQuery） |
| pageSize | Integer | 否 | Query | - | 每页条数（BaseQuery） |
| orderBy | String | 否 | Query | - | 排序字段（BaseQuery） |

- 请求示例：
```http
GET /admin/category/loadCategory
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.7 `/admin/category/saveCategory`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：saveCategory
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| pCategoryId | Integer | 是 | Query/Form | NotNull | - |
| categoryId | Integer | 否 | Query/Form | - | - |
| categoryCode | String | 是 | Query/Form | NotEmpty | - |
| categoryName | String | 是 | Query/Form | NotEmpty | - |
| icon | String | 否 | Query/Form | - | - |

- 请求示例：
```http
POST /admin/category/saveCategory
Content-Type: application/x-www-form-urlencoded

pCategoryId=<value>
categoryId=<value>
categoryCode=<value>
categoryName=<value>
icon=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.8 `/admin/file/getResource`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：getResource
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| sourceName | String | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
GET /admin/file/getResource?sourceName=<value>
```

- 成功响应示例：
（二进制流响应，非 JSON）
- 资源读取接口直接输出文件字节流。
- 成功时通常返回 200，并由 Content-Type 表示资源类型。
- 常见错误码：200 / 600 / 500

#### 5.4.9 `/admin/file/uploadImage`
- 建议方法：POST
- 登录要求（代码声明）：是
- 说明：uploadImage
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| file | MultipartFile | 是 | FormData | NotNull | - |
| createThumbnail | Boolean | 是 | Query/Form | NotNull | - |

- 请求示例：
```http
POST /admin/file/uploadImage
Content-Type: multipart/form-data

file=<binary>
createThumbnail=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500 / 901

#### 5.4.10 `/admin/file/videoResource/{fileId}`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：videoResource
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| fileId | String | 是 | Path | PathVariable("fileId") + NotEmpty | - |

- 请求示例：
```http
GET /admin/file/videoResource/{fileId}
```

- 成功响应示例：
（二进制流响应，非 JSON）
- 资源读取接口直接输出文件字节流。
- 成功时通常返回 200，并由 Content-Type 表示资源类型。
- 常见错误码：200 / 600 / 500

#### 5.4.11 `/admin/file/videoResource/{fileId}/{ts}`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：videoResourceTs
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| fileId | String | 是 | Path | PathVariable("fileId") + NotEmpty | - |
| ts | String | 是 | Path | PathVariable("ts") + NotEmpty | - |

- 请求示例：
```http
GET /admin/file/videoResource/{fileId}/{ts}
```

- 成功响应示例：
（二进制流响应，非 JSON）
- 资源读取接口直接输出文件字节流。
- 成功时通常返回 200，并由 Content-Type 表示资源类型。
- 常见错误码：200 / 600 / 500

#### 5.4.12 `/admin/videoInfo/auditVideo`
- 建议方法：POST
- 登录要求（代码声明）：否
- 说明：auditVideo
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | - |
| status | Integer | 是 | Query/Form | NotNull | - |
| reason | String | 否 | Query/Form | - | - |

- 请求示例：
```http
POST /admin/videoInfo/auditVideo
Content-Type: application/x-www-form-urlencoded

videoId=<value>
status=<value>
reason=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": null
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.13 `/admin/videoInfo/loadVideoList`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：loadVideoPost
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 否 | Query | - | 视频ID |
| videoIdFuzzy | String | 否 | Query | - | 视频ID模糊匹配 |
| videoCover | String | 否 | Query | - | 视频封面 |
| videoCoverFuzzy | String | 否 | Query | - | 视频封面模糊匹配 |
| videoName | String | 否 | Query | - | 视频标题 |
| videoNameFuzzy | String | 否 | Query | - | 视频标题模糊匹配 |
| userId | String | 否 | Query | - | UP主ID |
| userIdFuzzy | String | 否 | Query | - | UP主ID模糊匹配 |
| createTime | Date | 否 | Query | - | 创建时间 |
| createTimeStart | String | 否 | Query | - | 创建时间起 |
| createTimeEnd | String | 否 | Query | - | 创建时间止 |
| lastUpdateTime | Date | 否 | Query | - | 最后更新时间 |
| lastUpdateTimeStart | String | 否 | Query | - | 最后更新时间起 |
| lastUpdateTimeEnd | String | 否 | Query | - | 最后更新时间止 |
| pCategoryId | Integer | 否 | Query | - | 父分类ID |
| categoryId | Integer | 否 | Query | - | 分类ID |
| status | Integer | 否 | Query | - | 视频状态 |
| postType | Integer | 否 | Query | - | 投稿类型 |
| originInfo | String | 否 | Query | - | 来源说明 |
| originInfoFuzzy | String | 否 | Query | - | 来源说明模糊匹配 |
| tags | String | 否 | Query | - | 标签 |
| tagsFuzzy | String | 否 | Query | - | 标签模糊匹配 |
| introduction | String | 否 | Query | - | 简介 |
| introductionFuzzy | String | 否 | Query | - | 简介模糊匹配 |
| interaction | String | 否 | Query | - | 互动设置 |
| interactionFuzzy | String | 否 | Query | - | 互动设置模糊匹配 |
| duration | Integer | 否 | Query | - | 时长（秒） |
| excludeStatusList | Integer[] | 否 | Query | - | 排除状态集合 |
| queryCountInfo | Boolean | 否 | Query | - | 是否查询统计信息 |
| queryUserInfo | Boolean | 否 | Query | - | 是否关联用户信息 |
| pageNo | Integer | 否 | Query | - | 页码（BaseQuery） |
| pageSize | Integer | 否 | Query | - | 每页条数（BaseQuery） |
| orderBy | String | 否 | Query | - | 排序字段（BaseQuery） |

- 请求示例：
```http
GET /admin/videoInfo/loadVideoList
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "totalCount": 100,
    "pageSize": 15,
    "pageNo": 1,
    "pageTotal": 7,
    "list": []
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.14 `/admin/videoInfo/getAiAuditSummary`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：查询指定视频最新一次视频级 AI 审核任务摘要。管理端前端调用时会携带 `adminToken`。
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | 视频 ID |

- 请求示例：
```http
GET /admin/videoInfo/getAiAuditSummary?videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "taskId": 1,
    "requestId": "audit-v10001-1",
    "videoId": "v10001",
    "auditVersion": 1,
    "sourceType": 1,
    "taskStatus": 2,
    "aiDecision": 3,
    "aiRiskLevel": 2,
    "aiSummary": "存在中风险片段，建议人工复核",
    "modelName": "qwen-video-moderation",
    "modelVersion": "1.0.0",
    "triggerTime": "2026-05-27 10:00:00",
    "completedAt": "2026-05-27 10:02:00",
    "retryCount": 0,
    "lastError": null
  }
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.15 `/admin/videoInfo/getAiAuditItems`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：查询指定视频最新一次 AI 审核任务下的分 P / 文件级审核明细。`riskTagsJson` 与 `hitSegmentsJson` 为 JSON 字符串。
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | 视频 ID |

- 请求示例：
```http
GET /admin/videoInfo/getAiAuditItems?videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": [
    {
      "itemId": 1,
      "taskId": 1,
      "videoId": "v10001",
      "fileId": "f10001",
      "fileIndex": 0,
      "uploadId": "upload-001",
      "fileName": "第 1 P",
      "filePath": "video/20260527/f10001",
      "duration": 120,
      "updateType": 1,
      "itemStatus": 2,
      "itemDecision": 3,
      "riskScore": 0.5,
      "riskTagsJson": "[\"audio_event\"]",
      "hitSegmentsJson": "[{\"segmentId\":1,\"startSeconds\":0.0,\"endSeconds\":30.0}]",
      "itemReason": "中风险 - 建议人工复核",
      "createdAt": "2026-05-27 10:00:00",
      "updatedAt": "2026-05-27 10:02:00"
    }
  ]
}
```
- 常见错误码：200 / 600 / 500

#### 5.4.16 `/admin/videoInfo/getVideoPostDetail`
- 建议方法：GET
- 登录要求（代码声明）：否
- 说明：查询后台审核详情页所需的投稿主信息和投稿态分 P 文件列表。
- 请求参数：

| 参数 | 类型 | 必填 | 位置 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- |
| videoId | String | 是 | Query/Form | NotEmpty | 视频 ID |

- 请求示例：
```http
GET /admin/videoInfo/getVideoPostDetail?videoId=<value>
```

- 成功响应示例：
```json
{
  "status": "success",
  "code": 200,
  "info": "请求成功",
  "data": {
    "videoInfo": {
      "videoId": "v10001",
      "videoName": "示例投稿",
      "userId": "u10001",
      "status": 2,
      "aiDecision": 3,
      "aiRiskLevel": 2,
      "aiSummary": "存在中风险片段，建议人工复核"
    },
    "videoInfoFilePosts": [
      {
        "fileId": "f10001",
        "videoId": "v10001",
        "fileIndex": 0,
        "fileName": "第 1 P",
        "filePath": "video/20260527/f10001",
        "duration": 120,
        "transferResult": 1,
        "updateType": 1
      }
    ]
  }
}
```
- 常见错误码：200 / 600 / 500

## 6. AI 审核异步消息接口

### 6.1 RabbitMQ 通道
| 用途 | Exchange | Queue | Routing Key | DLX | DLQ |
| --- | --- | --- | --- | --- | --- |
| AI 审核请求 | `streama.audit.exchange` | `streama.audit.request.queue` | `audit.video.request` | `streama.audit.request.dlx` | `streama.audit.request.dlq` |
| AI 审核结果 | `streama.audit.exchange` | `streama.audit.result.queue` | `audit.video.result` | `streama.audit.result.dlx` | `streama.audit.result.dlq` |

说明：
- Web 服务在视频全部分 P 转码完成后创建 AI 审核任务，并向请求队列发送 `AuditRequestMessage`。
- AI 服务消费请求队列，完成审核后向结果队列发送 `AuditResultMessage`。
- Web 服务消费结果队列，回写 `ai_audit_task` 和 `ai_audit_task_item`。

### 6.2 `AuditRequestMessage`
| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| requestId | String | 是 | 审核请求唯一 ID |
| videoId | String | 是 | 视频 ID |
| auditVersion | Integer | 是 | 审核版本号 |
| sourceType | Integer | 是 | 来源类型，见 `sourceType` 字典 |
| triggerTime | Date | 是 | 触发时间 |
| videoMeta | Object | 是 | 视频投稿元数据 |
| items | Array | 是 | 待审核分 P / 文件列表 |

`videoMeta` 字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| userId | String | 投稿用户 ID |
| videoName | String | 视频标题 |
| videoCover | String | 视频封面 |
| tags | String | 标签 |
| introduction | String | 简介 |
| pCategoryId | Integer | 一级分类 ID |
| categoryId | Integer | 二级分类 ID |
| postType | Integer | 投稿类型 |

`items` 元素字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| fileId | String | 分 P 文件 ID |
| fileIndex | Integer | 分 P 序号 |
| uploadId | String | 上传任务 ID |
| fileName | String | 文件名 |
| filePath | String | 文件路径 |
| duration | Integer | 时长，单位秒 |
| updateType | Integer | 文件更新类型 |

请求消息示例：
```json
{
  "requestId": "audit-v10001-1",
  "videoId": "v10001",
  "auditVersion": 1,
  "sourceType": 1,
  "triggerTime": "2026-05-27 10:00:00",
  "videoMeta": {
    "userId": "u10001",
    "videoName": "示例投稿",
    "videoCover": "cover/20260527/demo.jpg",
    "tags": "测试,剪辑",
    "introduction": "视频简介",
    "pCategoryId": 1,
    "categoryId": 10,
    "postType": 0
  },
  "items": [
    {
      "fileId": "f10001",
      "fileIndex": 0,
      "uploadId": "upload-001",
      "fileName": "第 1 P",
      "filePath": "video/20260527/f10001",
      "duration": 120,
      "updateType": 1
    }
  ]
}
```

### 6.3 `AuditResultMessage`
| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| requestId | String | 是 | 对应请求 ID |
| videoId | String | 是 | 视频 ID |
| auditVersion | Integer | 是 | 审核版本号 |
| modelName | String | 否 | 模型名称 |
| modelVersion | String | 否 | 模型版本 |
| completedAt | Date | 是 | 完成时间 |
| videoDecision | Integer | 是 | 视频级 AI 建议，见 `aiDecision` 字典 |
| videoRiskLevel | Integer | 是 | 视频级风险等级，见 `aiRiskLevel` 字典 |
| videoSummary | String | 否 | 视频级审核摘要 |
| items | Array | 是 | 分 P / 文件级审核结果 |

`items` 元素字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| fileId | String | 分 P 文件 ID |
| itemStatus | Integer | 分 P 处理状态，见 `itemStatus` 字典 |
| itemDecision | Integer | 分 P AI 建议，见 `aiDecision` 字典 |
| riskScore | BigDecimal | 分 P 风险分数 |
| riskTags | Object | 风险标签集合 |
| hitSegments | Object | 命中片段集合 |
| itemReason | String | 分 P 审核说明 |

结果消息示例：
```json
{
  "requestId": "audit-v10001-1",
  "videoId": "v10001",
  "auditVersion": 1,
  "modelName": "qwen-video-moderation",
  "modelVersion": "1.0.0",
  "completedAt": "2026-05-27 10:02:00",
  "videoDecision": 3,
  "videoRiskLevel": 2,
  "videoSummary": "存在中风险片段，建议人工复核",
  "items": [
    {
      "fileId": "f10001",
      "itemStatus": 2,
      "itemDecision": 3,
      "riskScore": 0.5,
      "riskTags": ["audio_event"],
      "hitSegments": [
        {
          "segmentId": 1,
          "startSeconds": 0.0,
          "endSeconds": 30.0,
          "textPreview": "",
          "riskType": "audio_event",
          "riskScore": 0.5,
          "reason": "检测到风险声音事件",
          "hasRiskSound": true,
          "bestFramePath": "audit-snapshot/v10001/1/f10001_1.jpg"
        }
      ],
      "itemReason": "中风险 - 建议人工复核"
    }
  ]
}
```

### 6.4 AI 分 P 进度回调
当前代码提供内部进度回调模型 `AiAuditItemProgressMessage`，主要字段如下。该能力属于内部链路，不作为网关对外 HTTP 接口统计。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| requestId | String | 审核请求 ID |
| fileId | String | 分 P 文件 ID |
| itemStatus | Integer | 分 P 处理状态 |
| lastError | String | 最近错误信息 |
| updatedAt | Date | 更新时间 |

## 7. 联调示例（网关口径）
### 7.1 Web 登录
```http
POST /web/account/login
Content-Type: application/x-www-form-urlencoded

email=test@example.com&password=Pass1234&checkCodeKey=<key>&checkCode=<code>
```

### 7.2 投稿列表
```http
GET /web/ucenter/loadVideoPostList?status=2&pageNo=1
```

### 7.3 评论列表
```http
GET /interact/comment/loadComment?videoId=v10001&pageNo=1&orderType=0
```

### 7.4 用户行为
```http
POST /interact/userAction/doAction
Content-Type: application/x-www-form-urlencoded

videoId=v10001&actionType=2&actionCount=1
```

### 7.5 资源读取
```http
GET /file/getResource?sourceName=cover/20260417/demo.jpg
```

### 7.6 管理端审核
```http
POST /admin/videoInfo/auditVideo
Content-Type: application/x-www-form-urlencoded

videoId=v10001&status=3
```

### 7.7 管理端审核详情
```http
GET /admin/videoInfo/getVideoPostDetail?videoId=v10001
```

### 7.8 管理端 AI 审核摘要
```http
GET /admin/videoInfo/getAiAuditSummary?videoId=v10001
```

### 7.9 管理端 AI 审核明细
```http
GET /admin/videoInfo/getAiAuditItems?videoId=v10001
```

## 8. 变更记录
- 2026-05-27：补充管理端 AI 审核详情相关接口，Admin 对外接口数调整为 16。
- 2026-05-27：新增 AI 审核异步消息契约，记录 RabbitMQ 通道与请求/结果消息结构。
- 2026-04-17：按当前代码全量重构文档，接口口径统一为网关路径。
- 2026-04-17：接口明细覆盖 62/62 条对外路径。
- 2026-04-17：登录要求按 @GlobalInterceptor(checkLogin=true) 重新对齐。
- 2026-04-17：新增 Query 对象展开说明（CategoryInfoQuery、VideoInfoPostQuery、BaseQuery）。
