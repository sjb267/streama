# RabbitMQ broker-side delivery acknowledgement timeout.
# 4 hours in milliseconds. This fixes long AI audits being killed by the
# default 30 minute consumer_timeout before the service can ack the request.
consumer_timeout = 14400000
