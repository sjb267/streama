% 第五章 系统详细设计与实现
% 本章在第四章总体设计基础上，重点说明核心功能的实现方法。
% 已按“减少普通业务算法、突出视频上传转码与 AI 审核实现”的方向进行压缩整理。

\chapter{视频分享平台的设计与实现}

用户端功能模块是普通用户访问系统的主要入口，包括视频浏览、视频搜索、视频播放、用户认证和互动操作等功能。该模块并不是单一的数据读取模块，而是由主站业务服务、互动服务、缓存组件、检索组件和数据库共同协作完成。公开视频浏览、分类查询和视频搜索允许未登录用户访问，而点赞、收藏、投币、关注、评论和发送弹幕等操作需要用户完成登录认证后才能执行。

\section{用户端功能模块实现}

\subsection{用户端整体功能实现}

\begin{figure}[!htbp] 
    \centering 
    \includegraphics[width=0.95\textwidth]{images/user_core_interface_flow.png} 
    \caption{用户端核心接口处理时序图} 
    \label{fig:user-core-interface-flow} 
\end{figure} 

用户端功能模块主要给用户展示公开视频的数据读取、互动状态判断和登录校验等逻辑，系统如何完成视频列表查询、搜索结果返回、播放页数据组合等处理。 

首页的视频列表、分类查询、关键词搜索以及视频详情读取属于公开读取接口，公开读取接口允许未登录用户访问，但会统一过滤未发布、待审核、转码中或资源异常的视频数据。实行点赞、收藏、投币、关注、评论和弹幕发送属于登录写入类接口，系统会先解析请求令牌，并从 Redis 中读取用户登录信息；如果登录态不存在或已经过期，则直接返回未登录结果，不执行后续操作。 

用户端核心接口处理过程如图~\ref{fig:user-core-interface-flow} 所示。系统在接收到请求后则通过网关转发到对应业务服务。视频基础数据主要由主站业务服务查询，搜索请求结合 Elasticsearch 完成关键词匹配，评论、弹幕和用户行为由互动服务处理，播放资源则通过资源服务读取。各服务处理完成后，系统将视频主数据、分 P 文件、播放资源和互动状态封装为统一响应结果返回。

\subsection{首页浏览与视频搜索实现}

首页浏览与视频搜索接口都属于公开视频读取接口，但二者在代码中的入口和查询方式并不相同。首页浏览接口主要对应 \texttt{/web/video/loadVideoList}，该接口根据父级分类编号、分类编号、页码和分页大小构造 \texttt{VideoInfoQuery} 查询对象，并通过 \texttt{VideoInfoService} 返回分页视频列表。关键词搜索接口主要对应 \texttt{/web/video/search}，该接口接收关键词、排序类型和页码参数，先将关键词写入 Redis 热词统计，再通过 \texttt{EsSearchComponent} 完成搜索并返回分页结果。

首页浏览与视频搜索接口说明如表~\ref{tab:home-video-search-api} 所示。

\begin{table}[!htbp]
    \centering
    \caption{首页浏览与视频搜索接口说明}
    \label{tab:home-video-search-api}
    \begin{tabular}{p{0.35\textwidth} p{0.22\textwidth} p{0.38\textwidth}}
        \toprule
        \textbf{接口路径} & \textbf{主要参数} & \textbf{实现说明} \\
        \midrule
        /web/video/loadVideoList
        & \texttt{pCategoryId}、\texttt{categoryId}、\texttt{pageNo}、\texttt{pageSize} 
        & 根据分类和分页条件构造 \texttt{VideoInfoQuery}，调用 \texttt{VideoInfoService.findListByPage} 查询正式视频分页数据。 \\

        /web/video/search
        & \texttt{keyword}、\texttt{orderType}、\texttt{pageNo} 
        & 对关键词进行非空校验后写入 Redis 热词统计，并调用 \texttt{EsSearchComponent.search} 返回搜索分页结果。 \\

        /web/video/getSearchKeywordTop
        & 无 
        & 从 Redis 中读取搜索热词列表，用于搜索页或首页搜索提示展示。 \\
        \bottomrule
    \end{tabular}
\end{table}

首页浏览与视频搜索处理过程如图~\ref{fig:home-video-search-flow} 所示。系统首先判断请求中是否包含关键词：若不包含关键词，则按分类和分页条件查询 MySQL 中的公开视频数据；若包含关键词，则先通过 Elasticsearch 检索视频标题和标签等字段，同时将关键词写入 Redis 搜索热词统计。两条路径取得候选视频后，都会继续过滤视频状态，并补充作者、分类和统计数据，最后封装为视频摘要列表返回给用户端。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/home_video_search_flow.png}
    \caption{首页浏览与视频搜索处理流程图}
    \label{fig:home-video-search-flow}
\end{figure}

首页浏览与视频搜索实现过程如算法~\ref{alg:home-video-search-impl} 所示。

\begin{algorithm}[!htbp]
    \caption{首页浏览与视频搜索实现算法}
    \label{alg:home-video-search-impl}
    \begin{algorithmic}[1]
        \Require 分类条件 $C$，关键词 $keyword$，排序类型 $orderType$，页码 $pageNo$
        \Ensure 公开视频分页结果 $P$

        \If{$keyword$ 为空}
            \State 构造 \texttt{VideoInfoQuery} 查询对象 $Q$
            \State 将父级分类、子分类、页码和分页大小写入 $Q$
            \State 设置 $Q$ 只查询公开视频，并按创建时间倒序排序
            \State 调用 \texttt{VideoInfoService.findListByPage} 查询 MySQL，得到分页结果 $P$
        \Else
            \State 调用 \texttt{RedisComponent.addKeywordCount} 记录搜索热词
            \State 根据 $keyword$、$orderType$ 和 $pageNo$ 构造 Elasticsearch 检索条件
            \State 调用 \texttt{EsSearchComponent.search} 检索视频标题和标签字段，得到分页结果 $P$
        \EndIf

        \For{每一个视频摘要 $v \in P.list$}
            \State 过滤不可公开访问的视频状态
            \State 补充作者昵称、分类信息和播放量、弹幕量等统计字段
        \EndFor

        \State 将总记录数、当前页、总页数和视频摘要列表封装到 \texttt{PaginationResultVO}
        \State \Return $P$
    \end{algorithmic}
\end{algorithm}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/home_video_search_result.png}
    \caption{首页浏览与视频搜索结果界面}
    \label{fig:home-video-search-result}
\end{figure}

算法~\ref{alg:home-video-search-impl} 对应的前端实现效果如图~\ref{fig:home-video-search-result} 所示。用户在首页顶部搜索框输入关键词“低头”并提交后，系统进入搜索结果展示模式，页面显示当前搜索关键词、排序条件以及匹配到的视频列表。视频卡片中展示了视频封面、标题、作者信息以及播放量、弹幕量、收藏量等摘要数据，右上方的播放量、发布时间、弹幕量和收藏量按钮对应搜索接口中的排序参数，用于控制搜索结果的展示顺序。该界面说明后端返回的分页视频数据已经被前端统一渲染为可浏览、可排序、可分页的视频搜索结果。

首页浏览和关键词搜索虽然查询入口不同，但最终都使用 \texttt{PaginationResultVO} 封装分页结果，便于前端以统一方式渲染视频列表。接口返回的数据以 \texttt{VideoInfo} 为主要内容对象，通常包含视频编号、标题、封面、分类、作者、播放数、弹幕数、点赞数、收藏数和发布时间等展示字段。系统只允许正式发布且资源可访问的视频进入公开视频列表，未发布、待审核、转码中或资源异常的视频不会被返回。搜索热词只用于 Redis 统计用户搜索行为，不直接参与公开视频过滤。

\subsection{视频播放页实现}

视频播放页实现主要围绕视频详情、分 P 文件、用户行为状态和播放资源读取几个接口展开。与用户端整体接口时序不同，本节不再重复说明网关转发和服务协作过程，而是从播放页所依赖的接口和关键数据对象角度说明其实现方式。

视频播放页进入时，首先根据视频编号调用 \texttt{/web/video/getVideoInfo} 接口读取视频主信息。该接口内部通过 \texttt{VideoInfoService.getVideoInfoByVideoId} 查询正式视频数据；如果视频记录不存在，则直接抛出业务异常。若当前请求中能够解析出登录用户信息，系统还会构造 \texttt{UserActionQuery}，并通过互动服务查询当前用户对该视频的点赞、收藏和投币状态，最后将视频主信息和用户行为状态封装为 \texttt{VideoInfoResultVO} 返回。

视频播放页相关接口说明如表~\ref{tab:video-play-api} 所示。

\begin{table}[!htbp]
    \centering
    \caption{视频播放页相关接口说明}
    \label{tab:video-play-api}
    \begin{tabular}{p{0.34\textwidth} p{0.2\textwidth} p{0.38\textwidth}}
        \toprule
        \textbf{接口路径} & \textbf{主要参数} & \textbf{实现说明} \\
        \midrule
        /web/video/getVideoInfo
        & \texttt{videoId}
        & 查询视频主信息，并在用户已登录时补充点赞、收藏、投币等行为状态。 \\

        /web/video/loadVideoPList
        & \texttt{videoId}
        & 根据视频编号查询分 P 文件列表，并按照 \texttt{file\_index asc} 排序返回。 \\

        /resource/videoResource/\{fileId\}
        & \texttt{fileId}
        & 根据分 P 文件编号读取 HLS 的 \texttt{m3u8} 索引文件；正式视频访问时写入播放记录。 \\

        /resource/videoResource/\{fileId\}/\{ts\}
        & \texttt{fileId}、\texttt{ts}
        & 根据分 P 文件编号和切片名称读取对应的 \texttt{ts} 切片文件。 \\
        \bottomrule
    \end{tabular}
\end{table}

分 P 文件列表由 \texttt{/web/video/loadVideoPList} 接口返回。接口层根据视频编号构造 \texttt{VideoInfoFileQuery}，并设置排序条件为 \texttt{file\_index asc}，再调用 \texttt{VideoInfoFileServiceImpl.findListByParam} 查询分 P 文件数据。返回结果主要包含分 P 文件编号、视频编号、文件名称、文件序号、播放路径和视频时长等信息。播放页根据这些数据生成分 P 列表，并使用当前选中的 \texttt{fileId} 请求实际播放资源。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{images/video_play_class.png}
    \caption{视频播放页关键类图}
    \label{fig:video-play-class}
\end{figure}

如图~\ref{fig:video-play-class} 所示，播放页接口涉及主站业务服务和资源服务两部分。\texttt{VideoController} 负责提供视频详情和分 P 列表数据，\texttt{VideoInfoResultVO} 用于组合视频主信息和用户行为状态，\texttt{VideoInfoFileQuery} 用于按视频编号查询分 P 文件。\texttt{FileController} 负责根据 \texttt{fileId} 读取播放资源，系统会先通过 \texttt{VideoClient} 查询正式分 P 文件；如果未查询到正式文件，再查询投稿态分 P 文件，用于支持预览类场景。

播放资源并不直接暴露服务器真实目录。播放页只持有分 P 文件编号，资源服务根据 \texttt{fileId} 解析文件路径后读取 \texttt{index.m3u8} 和对应的 \texttt{ts} 切片文件。对于正式视频，资源服务在读取 \texttt{m3u8} 时会构造 \texttt{VideoPlayInfoDto}，并将视频编号、分 P 序号和可选的用户编号写入 Redis 播放记录队列。这样既能保证播放资源由统一接口输出，也能为后续播放量统计提供数据来源。

\section{创作中心与视频投稿实现}

创作中心与视频投稿实现主要围绕投稿态数据处理展开。项目中创作中心相关接口集中在 \texttt{CenterVideoPostController} 中，其访问路径以 \texttt{/ucenter} 为前缀，并通过登录拦截保证投稿数据与当前用户绑定。资源上传相关接口由资源服务中的 \texttt{FileController} 提供，投稿保存接口并不直接接收完整视频文件，而是接收视频基础信息和分 P 文件列表，其中分 P 文件通过 \texttt{uploadId} 与前置上传任务关联。

\subsection{创作中心功能结构}

创作中心功能结构在代码中主要体现为一组围绕投稿态视频的接口。稿件列表查询接口根据当前登录用户编号构造 \texttt{VideoInfoPostQuery}，并按照创建时间倒序返回分页结果；稿件数量统计接口分别统计审核通过、审核失败和处理中稿件数量；稿件详情接口根据视频编号读取 \texttt{VideoInfoPost} 和对应的 \texttt{VideoInfoFilePost} 列表，并返回给编辑页面。相关接口说明如表~\ref{tab:center-video-post-api} 所示。

\begin{table}[!htbp]
    \centering
    \caption{创作中心投稿相关接口说明}
    \label{tab:center-video-post-api}
    \begin{tabular}{p{0.32\textwidth} p{0.22\textwidth} p{0.38\textwidth}}
        \toprule
        \textbf{接口路径} & \textbf{主要参数} & \textbf{实现说明} \\
        \midrule
        \texttt{/ucenter/loadVideoPostList}
        & \texttt{status}、\texttt{pageNo}、\texttt{videoNameFuzzy}
        & 根据当前用户编号、稿件状态和标题模糊条件查询投稿态视频分页数据。 \\

        \texttt{/ucenter/getVideoCountInfo}
        & 无
        & 统计当前用户审核通过、审核失败和处理中稿件数量。 \\

        \texttt{/ucenter/getVideoByVideoId}
        & \texttt{videoId}
        & 校验视频归属后，读取投稿主信息和分 P 文件列表，用于编辑页回显。 \\

        \texttt{/ucenter/postVideo}
        & 投稿基础信息、\texttt{uploadFileList}
        & 保存投稿主信息和分 P 文件信息，并根据文件变化初始化稿件状态。 \\

        \texttt{/ucenter/saveVideoInteraction}
        & \texttt{videoId}、\texttt{interaction}
        & 保存视频评论、弹幕等互动开关配置。 \\

        \texttt{/ucenter/deleteVideo}
        & \texttt{videoId}
        & 根据视频编号和当前用户编号删除稿件或公开视频数据。 \\
        \bottomrule
    \end{tabular}
\end{table}

创作中心的核心对象关系如图~\ref{fig:center-video-post-class} 所示。\texttt{CenterVideoPostController} 负责接收创作中心请求，并将用户编号写入查询对象或投稿对象中；\texttt{VideoInfoPostService} 负责处理投稿主记录保存、分页查询和状态更新；\texttt{VideoInfoFilePostService} 负责读取分 P 文件列表；\texttt{VideoPostEditInfo} 用于组合投稿主信息和分 P 文件信息，返回给编辑页面。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{images/center_video_post_class.png}
    \caption{创作中心投稿关键类图}
    \label{fig:center-video-post-class}
\end{figure}

在数据层面，创作中心主要操作投稿态表，而不是直接操作正式公开视频表。\texttt{VideoInfoPost} 保存投稿视频的标题、封面、分类、标签、简介、互动配置和投稿状态等信息；\texttt{VideoInfoFilePost} 保存每个分 P 文件的上传标识、文件编号、文件名称、分 P 顺序、转码结果和文件路径等信息。一个投稿视频可以对应多个分 P 文件，因此编辑页面需要同时读取这两类数据。

\subsection{视频分片上传实现}

视频分片上传由资源服务处理，主要涉及 \texttt{/preUploadVideo}、\texttt{/uploadVideo} 和 \texttt{/deleteUploadVideo} 三个接口。上传前，系统根据文件名和分片数量创建上传任务，并在 Redis 中保存 \texttt{UploadFileDto}；上传过程中，每个分片请求都携带 \texttt{uploadId} 和 \texttt{chunkIndex}，资源服务根据当前用户编号和 \texttt{uploadId} 读取 Redis 中的上传任务，再将分片写入临时目录。

视频分片上传相关接口说明如表~\ref{tab:video-upload-api} 所示。

\begin{table}[!htbp]
    \centering
    \caption{视频分片上传接口说明}
    \label{tab:video-upload-api}
    \begin{tabular}{p{0.3\textwidth} p{0.24\textwidth} p{0.38\textwidth}}
        \toprule
        \textbf{接口路径} & \textbf{主要参数} & \textbf{实现说明} \\
        \midrule
        \texttt{/preUploadVideo}
        & \texttt{fileName}、\texttt{chunks}
        & 创建上传任务，将文件名、分片数和临时路径等信息写入 Redis，并返回 \texttt{uploadId}。 \\

        \texttt{/uploadVideo}
        & \texttt{chunkFile}、\texttt{chunkIndex}、\texttt{uploadId}
        & 校验上传任务、文件大小和分片序号后，将当前分片保存到临时目录，并更新 Redis 中的上传进度。 \\

        \texttt{/deleteUploadVideo}
        & \texttt{uploadId}
        & 删除 Redis 中的上传任务，并清理对应临时上传目录。 \\
        \bottomrule
    \end{tabular}
\end{table}

视频分片上传过程如图~\ref{fig:video-chunk-upload-sequence} 所示。该图只描述资源上传阶段，不继续展开文件合并和转码处理，避免与后续“视频转码与资源服务实现”部分重复。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/video_chunk_upload_sequence.png}
    \caption{视频分片上传时序图}
    \label{fig:video-chunk-upload-sequence}
\end{figure}

上传阶段产生的 \texttt{uploadId} 并不等同于视频编号。它只表示一次临时文件上传任务，真正的视频编号和分 P 文件编号要在投稿保存阶段生成。这样可以把“文件是否上传完成”和“投稿信息是否保存”分开处理，避免用户只上传了临时文件但没有提交投稿时，系统直接生成业务视频记录。

\subsection{投稿信息保存与状态初始化实现}

投稿保存入口为 \texttt{/ucenter/postVideo}。接口接收视频编号、封面、标题、分类、投稿类型、标签、简介、互动配置和 \texttt{uploadFileList} 等参数，其中 \texttt{uploadFileList} 会被反序列化为 \texttt{VideoInfoFilePost} 列表。控制器设置当前用户编号后，调用 \texttt{VideoInfoPostService.saveVideoInfo} 完成投稿保存。

投稿保存的关键不是简单写入一条视频记录，而是根据新增投稿和编辑投稿两种情况分别处理。若请求中没有 \texttt{videoId}，系统生成新的视频编号，将投稿主记录初始化为转码中状态，并插入 \texttt{VideoInfoPost}。若请求中存在 \texttt{videoId}，系统先查询原有投稿数据，并拒绝处于转码中或待审核状态的视频继续编辑，避免异步转码或审核结果覆盖新的投稿内容。

投稿信息保存与状态初始化过程如图~\ref{fig:post-video-save-flow} 所示。

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.95\textwidth]{images/post_video_save_flow.png}
    \caption{投稿信息保存与状态初始化流程图}
    \label{fig:post-video-save-flow}
\end{figure}

\FloatBarrier

编辑投稿时，系统会读取数据库中已有的分 P 文件列表，并与本次提交的 \texttt{uploadFileList} 进行比较。若数据库中的某个分 P 文件在本次提交列表中不存在，说明该分 P 被删除，系统会删除对应的投稿态文件记录，并将文件路径写入 Redis 删除队列；若本次提交中存在 \texttt{fileId} 为空的分 P，说明它是新增文件，系统会生成新的文件编号，并将转码结果初始化为转码中。若没有新增文件，但标题、封面、标签、简介、投稿类型或分 P 文件名发生变化，则视频状态会被更新为待审核。

所有分 P 文件处理完成后，系统调用批量新增或更新方法保存 \texttt{VideoInfoFilePost} 列表。对于新增的分 P 文件，系统还会补充用户编号和视频编号，并调用 \texttt{RedisComponent.addFile2TransferQueue} 写入转码队列。资源服务后续从该队列中取出文件任务，完成临时分片合并和播放资源生成。通过这种方式，投稿接口只负责业务数据保存和状态初始化，不等待耗时的视频转码处理完成。

\section{视频转码与资源服务实现}

视频转码与资源服务实现主要处理投稿视频文件从临时分片资源到可播放资源的转换过程。投稿保存阶段已经将新增分 P 文件写入 Redis 转码队列，本节不再重复投稿侧的状态初始化逻辑，而是围绕资源服务内部的队列消费、分片合并、视频转码、HLS 文件生成和播放资源读取展开说明。

在项目实现中，资源服务并不通过同步接口等待视频处理完成，而是由后台任务持续消费 Redis 中的转码任务。每个转码任务对应一个投稿态分 P 文件，任务对象中包含用户编号、视频编号、文件编号和上传标识等信息。资源服务根据这些字段定位上传阶段产生的临时目录，并在处理完成后通过主站服务回写文件转码结果。

\subsection{资源服务职责}

资源服务的职责主要集中在文件资源本身，不直接决定视频是否可以发布。它负责从 Redis 转码队列中读取待处理分 P 文件，完成临时目录迁移、分片合并、视频信息读取、编码兼容处理和 HLS 播放资源生成，并将处理结果回写给主站业务服务。播放阶段，资源服务还负责根据分 P 文件编号读取 \texttt{m3u8} 索引文件和 \texttt{ts} 切片文件。

资源服务中的关键类和职责如表~\ref{tab:resource-service-core-class} 所示。

\begin{table}[!htbp]
    \centering
    \caption{资源服务关键类说明}
    \label{tab:resource-service-core-class}
    \begin{tabular}{p{0.28\textwidth} p{0.3\textwidth} p{0.35\textwidth}}
        \toprule
        \textbf{类名} & \textbf{核心方法} & \textbf{实现说明} \\
        \midrule
        \texttt{ExecuteQueueTask}
        & \texttt{consumeTransferFileQueue}
        & 后台循环读取 Redis 转码队列，取出待处理的 \texttt{VideoInfoFilePost} 后交给转码组件处理。 \\

        \texttt{TransferFileComponent}
        & \texttt{transferVideoFile}
        & 根据上传标识读取临时文件信息，完成目录迁移、分片合并、视频时长读取、转码和结果回写。 \\

        \texttt{FFmpegUtils}
        & \texttt{getVideoInfoDuration}、\texttt{getVideoCodec}、\texttt{convertVideo2Ts}
        & 封装 FFmpeg 和 FFprobe 命令，用于读取视频时长、获取编码格式、处理 HEVC 编码和生成 HLS 资源。 \\

        \texttt{FileController}
        & \texttt{videoResource}、\texttt{videoResourceTs}
        & 根据分 P 文件编号读取 \texttt{m3u8} 和 \texttt{ts} 文件，并在正式视频访问时写入播放记录。 \\

        \texttt{VideoClient}
        & \texttt{transferVideoFile4Db}、\texttt{getVideoInfoFileByFileId}
        & 作为资源服务访问主站视频数据的客户端，用于回写转码结果和查询分 P 文件路径。 \\
        \bottomrule
    \end{tabular}
\end{table}

资源服务关键类之间的关系如图~\ref{fig:resource-service-class} 所示。\texttt{ExecuteQueueTask} 负责从 Redis 中获取转码任务，\texttt{TransferFileComponent} 负责实际文件处理，\texttt{FFmpegUtils} 负责执行视频处理命令，\texttt{FileController} 负责播放资源输出，\texttt{VideoClient} 则用于资源服务与主站视频数据之间的交互。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{images/resource_service_class.png}
    \caption{资源服务关键类图}
    \label{fig:resource-service-class}
\end{figure}

通过上述划分，资源服务内部形成了较为清晰的处理边界。队列任务只负责触发处理，转码组件负责文件转换，工具类负责执行 FFmpeg 命令，控制器负责播放资源读取，主站服务负责保存最终业务状态。

\subsection{视频文件合并与转码实现}

视频文件合并与转码由 \texttt{TransferFileComponent.transferVideoFile} 完成。资源服务从队列中取得分 P 文件任务后，首先根据用户编号和 \texttt{uploadId} 从 Redis 中读取上传任务信息，并定位临时上传目录。随后，系统将临时目录复制到正式视频资源目录，删除临时目录和 Redis 中的上传任务信息，避免临时文件长期占用空间。

分片合并时，系统按照分片序号依次读取临时目录中的分片文件，并写入完整视频文件。合并完成后，系统通过 FFprobe 读取视频时长，并记录完整文件大小和正式资源路径。如果处理过程中出现目录不存在、分片缺失或命令执行异常，系统会将该分 P 文件的转码结果设置为失败，并在最后统一回写主站服务。

在编码处理方面，系统会先调用 \texttt{FFmpegUtils.getVideoCodec} 读取视频编码格式。若检测到视频编码为 HEVC，则先将其转换为 H.264 编码，再继续生成 HLS 播放资源；若视频编码已经符合处理要求，则直接进入 HLS 生成步骤。这样处理主要是为了减少部分浏览器或播放环境对 HEVC 编码支持不足的问题。

无论转码成功还是失败，资源服务最终都会调用 \texttt{VideoClient.transferVideoFile4Db} 将处理结果回写给主站服务。成功时回写视频时长、文件大小、资源路径和成功状态；失败时回写失败状态。主站服务再根据同一视频下所有分 P 文件的处理结果，决定投稿视频后续是否进入待审核状态。

视频文件合并与转码实现过程如算法~\ref{alg:video-merge-transcode-impl} 所示。

\begin{algorithm}[!htbp]
    \caption{视频文件合并与转码实现算法}
    \label{alg:video-merge-transcode-impl}
    \begin{algorithmic}[1]
        \Require Redis 转码队列任务 $T$
        \Ensure 分 P 文件转码结果 $R$

        \State 从 Redis 转码队列读取分 P 文件任务 $T$
        \If{$T$ 为空}
            \State 等待一段时间后继续轮询转码队列
            \State \Return 空结果
        \EndIf

        \State 初始化 $R.transferResult \leftarrow \texttt{FAIL}$
        \State 根据 $T.userId$ 和 $T.uploadId$ 读取上传任务信息 $U$
        \State 定位临时上传目录 $tempDir$ 和正式资源目录 $targetDir$
        \State 将 $tempDir$ 复制到 $targetDir$，并清理临时目录和 Redis 上传状态
        \State 按照分片序号依次读取 $targetDir$ 中的分片文件
        \State 将所有分片写入完整视频文件 $videoPath$
        \State 使用 FFprobe 读取视频时长 $duration$，并统计文件大小 $fileSize$
        \State 设置 $R.duration$、$R.fileSize$ 和 $R.filePath$
        \State 调用 \texttt{FFmpegUtils.getVideoCodec} 获取视频编码 $codec$
        \If{$codec$ 为 \texttt{HEVC}}
            \State 使用 FFmpeg 将视频转换为 H.264 编码文件
        \EndIf
        \State 使用 FFmpeg 生成 HLS 的 \texttt{index.m3u8} 和 \texttt{ts} 切片文件
        \State 设置 $R.transferResult \leftarrow \texttt{SUCCESS}$

        \If{处理过程中出现目录缺失、分片缺失或命令执行异常}
            \State 保持 $R.transferResult \leftarrow \texttt{FAIL}$
        \EndIf
        \State 调用 \texttt{VideoClient.transferVideoFile4Db} 回写 $R$
        \State \Return $R$
    \end{algorithmic}
\end{algorithm}

\subsection{HLS 播放资源生成与访问实现}

HLS 播放资源生成由 \texttt{FFmpegUtils.convertVideo2Ts} 完成。系统先将完整视频转换为中间 \texttt{index.ts} 文件，再按照固定时间切分为多个 \texttt{ts} 片段，并生成 \texttt{index.m3u8} 索引文件。项目中切片时间设置为 10 秒，播放器访问视频时先读取 \texttt{m3u8} 索引文件，再根据索引继续请求对应的 \texttt{ts} 切片。

播放资源访问由资源服务的 \texttt{FileController} 提供。播放页并不直接持有服务器文件真实路径，而是携带分 P 文件编号访问 \texttt{/videoResource/\{fileId\}}。资源服务根据 \texttt{fileId} 先查询正式分 P 文件；如果正式文件不存在，则继续查询投稿态分 P 文件，用于支持预览类访问。若两类数据都不存在，资源服务返回 404 状态。

HLS 播放资源访问过程如图~\ref{fig:hls-resource-sequence} 所示。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/hls_resource_sequence.png}
    \caption{HLS 播放资源访问时序图}
    \label{fig:hls-resource-sequence}
\end{figure}

当请求的是 \texttt{/videoResource/\{fileId\}} 时，资源服务读取对应目录下的 \texttt{index.m3u8} 文件并写入响应。若该文件属于正式发布视频，系统还会构造 \texttt{VideoPlayInfoDto}，保存视频编号、分 P 序号和可选的用户编号，并写入 Redis 播放记录队列。后续统计任务可以基于该队列异步更新播放量，避免每次播放请求都直接修改数据库。

当播放器继续请求 \texttt{/videoResource/\{fileId\}/\{ts\}} 时，资源服务根据同一个 \texttt{fileId} 解析资源目录，再读取对应的 \texttt{ts} 切片文件。该接口只负责切片文件读取，不重复写入播放记录。通过这种方式，系统既能够隐藏服务器真实文件路径，也能够将播放统计控制在索引文件访问阶段，减少切片频繁请求带来的额外统计开销。

\section{互动功能模块实现}

互动功能模块主要由互动服务承担，代码中对应评论、弹幕和用户行为三类接口。评论和弹幕属于内容型互动，需要保存用户提交的文本内容以及所属视频或分 P 文件；点赞、收藏、投币和评论赞踩属于行为型互动，主要通过用户编号、视频编号、评论编号和行为类型记录用户操作。由于互动功能在系统中属于基础业务支撑，本节不再展开完整业务流程，而是从接口、数据对象和统计字段更新角度进行说明。

互动模块中的主要接口如表~\ref{tab:interact-api} 所示。其中，评论和弹幕的读取接口允许未登录用户访问，但发送评论、发送弹幕以及执行用户行为操作需要登录校验。系统在写入互动数据前，会先根据视频编号读取视频信息，并判断视频是否存在以及评论区或弹幕功能是否被关闭。

\begin{table}[!htbp]
    \centering
    \caption{互动功能主要接口说明}
    \label{tab:interact-api}
    \begin{tabular}{p{0.28\textwidth} p{0.24\textwidth} p{0.4\textwidth}}
        \toprule
        \textbf{接口路径} & \textbf{主要参数} & \textbf{实现说明} \\
        \midrule
        \texttt{/comment/postComment}
        & \texttt{videoId}、\texttt{content}、\texttt{replyCommentId}
        & 保存评论或回复评论，写入评论表，并在顶级评论新增时更新视频评论数。 \\

        \texttt{/comment/loadComment}
        & \texttt{videoId}、\texttt{pageNo}、\texttt{orderType}
        & 根据视频编号分页查询评论，并在用户已登录时补充评论点赞或点踩状态。 \\

        \texttt{/danmu/postDanmu}
        & \texttt{videoId}、\texttt{fileId}、\texttt{text}、\texttt{time}
        & 保存弹幕内容，记录分 P 文件编号和播放时间，并更新视频弹幕数。 \\

        \texttt{/danmu/loadDanmu}
        & \texttt{videoId}、\texttt{fileId}
        & 按分 P 文件编号加载弹幕列表，并按照弹幕编号升序返回。 \\

        \texttt{/userAction/doAction}
        & \texttt{videoId}、\texttt{actionType}、\texttt{actionCount}、\texttt{commentId}
        & 统一处理视频点赞、收藏、投币以及评论赞踩等用户行为。 \\
        \bottomrule
    \end{tabular}
\end{table}

互动模块中的关键类关系如图~\ref{fig:interact-core-class} 所示。控制器层负责接收页面请求并解析当前用户信息，服务层负责完成业务校验、行为写入和统计字段更新，数据对象则分别对应评论、弹幕和用户行为记录。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{images/interact_core_class.png}
    \caption{互动功能关键类图}
    \label{fig:interact-core-class}
\end{figure}

\subsection{评论与弹幕实现}

评论功能由 \texttt{VideoCommentController} 和 \texttt{VideoCommentService} 实现。用户发表评论时，接口会接收视频编号、评论内容、回复评论编号和可选图片路径，并从登录信息中补充用户编号、昵称和头像。服务层首先根据视频编号查询视频信息，如果视频不存在或评论区已关闭，则直接返回异常；如果是回复评论，则继续查询被回复评论，确定父评论编号、被回复用户编号和被回复用户昵称等信息。评论写入成功后，若该评论为顶级评论，系统会调用主站服务更新视频评论数。

评论读取接口根据视频编号构造 \texttt{VideoCommentQuery}，并支持按点赞数或评论时间排序。查询时系统可以加载一级评论及其子评论，用于播放页评论区展示。若当前用户已登录，系统还会根据用户编号和视频编号查询评论点赞、点踩记录，并将这些行为状态与评论分页结果一起返回。这样页面可以直接展示用户是否已经对某条评论执行过点赞或点踩操作。

弹幕功能由 \texttt{VideoDanmuController} 和 \texttt{VideoDanmuService} 实现。发送弹幕时，系统保存视频编号、分 P 文件编号、弹幕内容、显示模式、颜色、播放时间和用户编号。与评论不同，弹幕必须绑定具体的 \texttt{fileId} 和播放时间，因此多分 P 视频可以分别加载各自的弹幕数据。服务层在保存弹幕前同样会查询视频信息，并判断视频是否关闭弹幕功能。弹幕保存成功后，系统更新视频弹幕数和搜索文档中的弹幕统计字段。

评论与弹幕处理过程如算法~\ref{alg:comment-danmu-impl} 所示。

\begin{algorithm}[!htbp]
    \caption{评论与弹幕处理实现算法}
    \label{alg:comment-danmu-impl}
    \begin{algorithmic}[1]
        \Require 互动请求 $request$，当前登录用户 $user$
        \Ensure 互动处理结果 $result$

        \If{$user$ 为空}
            \State 返回未登录异常，不写入互动数据
        \EndIf
        \State 根据 $request.videoId$ 查询视频信息 $video$
        \If{$video$ 不存在}
            \State 返回业务异常
        \EndIf

        \If{$request.type$ 为评论}
            \If{$video.interaction$ 表示评论区关闭}
                \State 返回评论区关闭异常
            \EndIf
            \State 构造评论对象 $comment$，写入视频编号、用户编号、昵称、头像和评论内容
            \If{$request.replyCommentId$ 不为空}
                \State 查询被回复评论 $parent$
                \State 根据 $parent$ 确定一级评论编号、被回复用户编号和被回复用户信息
            \Else
                \State 设置 $comment.pCommentId \leftarrow 0$
            \EndIf
            \State 写入评论记录
            \If{$comment.pCommentId = 0$}
                \State 调用主站服务增加视频评论数
            \EndIf
        \ElsIf{$request.type$ 为弹幕}
            \If{$video.interaction$ 表示弹幕关闭}
                \State 返回弹幕关闭异常
            \EndIf
            \State 构造弹幕对象 $danmu$
            \State 写入视频编号、分 P 文件编号、弹幕内容、颜色、模式、播放时间和用户编号
            \State 写入弹幕记录
            \State 调用主站服务增加视频弹幕数
            \State 调用搜索组件同步更新搜索文档中的弹幕统计字段
        \EndIf

        \State 返回处理成功结果 $result$
    \end{algorithmic}
\end{algorithm}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/comment_danmu_player_result.png}
    \caption{视频播放页弹幕展示界面}
    \label{fig:comment-danmu-player-result}
\end{figure}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/comment_danmu_comment_result.png}
    \caption{视频播放页评论区展示界面}
    \label{fig:comment-danmu-comment-result}
\end{figure}

效果如图~\ref{fig:comment-danmu-player-result} 和图~\ref{fig:comment-danmu-comment-result} 所示。图~\ref{fig:comment-danmu-player-result} 展示了视频播放页中的弹幕功能，用户可以在播放器下方输入弹幕内容，选择弹幕显示方式和颜色后发送，已发送的弹幕会按照播放时间叠加显示在视频画面上。图~\ref{fig:comment-danmu-comment-result} 展示了评论区功能，页面支持发布评论、上传评论图片、按热度或时间排序，并提供评论点赞、点踩、回复、置顶和删除等交互操作。上述界面分别对应算法中的弹幕处理分支和评论处理分支，说明系统能够将互动接口处理结果渲染为可直接操作的播放页互动功能。

\subsection{点赞、收藏、投币与关注实现}

点赞、收藏和投币统一由 \texttt{/userAction/doAction} 接口处理。接口接收视频编号、行为类型、行为数量和评论编号等参数，并在登录校验通过后构造 \texttt{UserAction} 对象。服务层首先根据视频编号查询视频信息，再根据 \texttt{actionType} 判断具体行为类型。如果行为类型不存在，系统会直接返回业务异常，避免非法行为类型写入数据库。

视频点赞和视频收藏属于可取消行为。系统会根据视频编号、评论编号、行为类型和用户编号查询已有行为记录：若记录已经存在，则删除原行为并将统计字段减一；若记录不存在，则新增行为并将统计字段加一。对于收藏行为，系统还会同步更新搜索文档中的收藏统计，使搜索排序或热度展示能够反映收藏变化。

投币行为与点赞、收藏不同，通常不作为可取消行为处理。系统会先判断用户是否给自己的视频投币，若是则返回异常；随后判断是否已经投过币，避免重复投币。投币前还需要扣减当前用户的币数，并给视频作者增加相应币数。只有这些操作成功后，系统才写入投币行为记录，并更新视频投币统计。该部分使用全局事务控制，主要是为了保证行为记录、用户币数和视频统计之间保持一致。

从当前代码中的行为枚举看，已实现的用户行为主要包括视频点赞、视频收藏、视频投币、评论点赞和评论点踩等。关注功能如果后续加入，可以继续沿用类似的用户关系记录方式，即以当前用户编号和目标用户编号作为唯一关系进行新增或取消；但在本文实现说明中，重点仍放在已有的 \texttt{UserAction} 行为处理逻辑上。

\subsection{互动统计数据更新实现}

互动统计数据主要由互动服务在写入行为时同步更新。评论新增时，系统只在顶级评论写入后增加视频评论数；回复评论属于评论内部层级变化，不单独增加视频维度的评论统计。删除顶级评论时，系统会减少视频评论数，并删除该评论下的二级评论，避免子评论失去父级关系。

弹幕新增后，系统会增加视频弹幕数，并更新搜索文档中的弹幕统计。弹幕读取时只根据分 P 文件编号加载，不会重复修改统计数据。这样可以区分“发送弹幕”与“读取弹幕”两类操作，避免播放页频繁加载弹幕时影响统计字段。

点赞和收藏通过行为记录是否存在来决定新增或取消，并同步更新视频表中的对应统计字段。评论点赞和点踩还存在互斥关系，用户点赞某条评论时，如果之前已经点踩，系统会先删除对立行为，再更新评论表中的点赞数和点踩数。投币则涉及用户币数扣减、作者币数增加、投币行为记录写入和视频投币数更新，因此比普通点赞和收藏需要更严格的一致性控制。

整体来看，互动统计更新并不是单独的统计任务，而是与评论、弹幕和用户行为写入同时完成。明细表用于记录用户具体做过什么操作，统计字段用于减少页面展示时的聚合查询成本。对于本项目规模而言，这种同步更新方式实现较为直接，也能够满足播放页、首页和搜索结果中的基础统计展示需要。

\section{后台管理功能实现}

后台管理功能主要用于处理管理员登录、分类维护、投稿内容查询和视频审核等操作。由于第三章和第四章已经对后台管理端的功能需求和服务划分进行了说明，本节不再重复描述后台管理端“能够做什么”，而是从管理端接口和关键业务类角度说明其实现方式。

在项目代码中，后台管理端主要由 \texttt{streama-cloud-admin} 模块提供接口，外部访问路径以 \texttt{/admin} 为前缀。管理员账号相关接口由 \texttt{AccountController} 处理，分类维护接口由 \texttt{CategoryController} 处理，视频内容和审核相关接口由 \texttt{VideoInfoController} 处理。其中，视频审核相关数据并不直接由后台服务独立维护，而是通过 \texttt{WebClient} 调用主站业务服务完成投稿视频查询、AI 审核结果读取和人工审核处理。

\subsection{后台管理端整体实现}

后台管理端整体实现主要包括管理员验证码生成、管理员登录、后台令牌保存和管理接口调用等内容。管理员登录前，系统通过 \texttt{/admin/account/checkCode} 生成验证码，并将验证码内容保存到 Redis 中。管理员提交账号、密码和验证码后，\texttt{AccountController.login} 会先校验验证码，再校验配置文件中的管理员账号和密码。校验通过后，系统生成后台登录令牌，并将其写入 Cookie，同时在 Redis 中保存管理员登录信息。

后台管理端账号相关接口如表~\ref{tab:admin-account-api} 所示。

\begin{table}[!htbp]
    \centering
    \caption{后台管理端账号接口说明}
    \label{tab:admin-account-api}
    \begin{tabular}{p{0.32\textwidth} p{0.24\textwidth} p{0.36\textwidth}}
        \toprule
        \textbf{接口路径} & \textbf{主要参数} & \textbf{实现说明} \\
        \midrule
        \texttt{/admin/account/checkCode}
        & 无
        & 使用 Kaptcha 生成验证码图片，将验证码文本写入 Redis，并返回验证码图片和验证码标识。 \\

        \texttt{/admin/account/login}
        & \texttt{account}、\texttt{password}、\texttt{checkCodeKey}、\texttt{checkCode}
        & 校验验证码、管理员账号和密码，登录成功后生成后台令牌并保存到 Redis。 \\

        \texttt{/admin/account/logout}
        & 无
        & 清理后台登录 Cookie，使管理员退出后台管理端。 \\
        \bottomrule
    \end{tabular}
\end{table}

后台管理端关键类关系如图~\ref{fig:admin-manage-class} 所示。该图只展示本节涉及的主要控制器、服务调用对象和数据对象，不再展开网关转发过程，避免与前文系统总体架构图重复。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{images/admin_manage_class.png}
    \caption{后台管理功能关键类图}
    \label{fig:admin-manage-class}
\end{figure}

从整体实现上看，后台管理端可以分为本地处理和远程调用两类逻辑。管理员登录和分类维护主要由后台管理服务内部完成；视频投稿列表、审核详情、AI 审核摘要和人工审核操作则通过 \texttt{WebClient} 转发给主站业务服务处理。这样可以避免后台服务直接维护投稿视频表和 AI 审核任务表，使视频审核状态仍然由主站业务流程统一管理。

\subsection{分类与内容管理实现}

分类管理由 \texttt{CategoryController} 提供接口，主要包括分类加载、分类保存、分类删除和分类排序调整。分类加载接口会构造 \texttt{CategoryInfoQuery} 查询对象，并设置排序条件为 \texttt{sort asc}，同时将查询结果转换为树形结构返回。这样后台页面可以按照一级分类和二级分类的层级关系展示分类数据。

分类维护相关接口如表~\ref{tab:admin-category-api} 所示。

\begin{table}[!htbp]
    \centering
    \caption{分类管理接口说明}
    \label{tab:admin-category-api}
    \begin{tabular}{p{0.3\textwidth} p{0.26\textwidth} p{0.36\textwidth}}
        \toprule
        \textbf{接口路径(admin)} & \textbf{主要参数} & \textbf{实现说明} \\
        \midrule
        \texttt{/category/loadCategory}
        & \texttt{pCategoryId}、\texttt{categoryNameFuzzy}、\texttt{pageNo}
        & 查询分类数据，并按照排序字段组织为分类树结构。 \\

        \texttt{/category/saveCategory}
        & \texttt{pCategoryId}、\texttt{categoryId}、\texttt{categoryCode}、\texttt{categoryName}、\texttt{icon}
        & 新增或修改分类信息，由 \texttt{CategoryInfoService.saveCategory} 完成保存。 \\

        \texttt{/category/delCategory}
        & \texttt{categoryId}
        & 根据分类编号删除分类，由服务层完成删除前的业务校验。 \\

        \texttt{/category/changeSort}
        & \texttt{pCategoryId}、\texttt{categoryIds}
        & 按照传入的分类编号顺序调整同级分类排序。 \\
        \bottomrule
    \end{tabular}
\end{table}

内容管理主要指后台对投稿视频列表的查询。后台接口 \texttt{\seqsplit{WebClient.loadVideoList}} 接收 \texttt{VideoInfoPostQuery} 查询对象，可以按照视频编号、标题、用户编号、分类、投稿状态、投稿类型和时间范围等条件查询投稿态视频数据。该接口本身不直接处理审核逻辑，而是用于为后台列表页提供分页数据。

从实现角度看，分类管理是后台服务本地业务，内容管理则通过 \texttt{\seqsplit{WebClient.loadVideoList}} 调用主站服务查询投稿数据。这样分类维护和视频投稿查询在代码中保持了比较清晰的边界，也避免后台管理模块重复实现视频投稿查询逻辑。

\subsection{视频审核管理实现}

视频审核管理是后台管理功能中相对重要的部分。后台视频审核入口位于 \texttt{\seqsplit{VideoInfoController}}，该控制器并不直接操作投稿视频表，而是通过 \texttt{\seqsplit{WebClient}} 调用主站业务服务完成审核相关处理。后台页面加载审核列表时，调用 \texttt{\seqsplit{WebClient.loadVideoList}} 查询投稿态视频；查看某个视频详情时，调用 \texttt{\seqsplit{WebClient.getVideoPostDetail}} 获取投稿主信息和分 P 文件信息；查看 AI 审核结果时，则分别调用 \texttt{\seqsplit{WebClient.getAiAuditSummary}} 和 \texttt{\seqsplit{WebClient.getAiAuditItems}} 获取视频级审核摘要和分 P 级审核明细。

视频审核相关接口如表~\ref{tab:admin-video-audit-api} 所示。

\begin{table}[!htbp]
    \centering
    \caption{视频审核管理接口说明}
    \label{tab:admin-video-audit-api}
    \begin{tabular}{p{0.34\textwidth} p{0.22\textwidth} p{0.36\textwidth}}
        \toprule
        \textbf{接口路径(admin)} & \textbf{主要参数} & \textbf{实现说明} \\
        \midrule
        \texttt{/videoInfo/loadVideoList}
        & \texttt{status}、\texttt{videoNameFuzzy}、\texttt{userId}、\texttt{pageNo}
        & 查询投稿态视频分页列表，用于后台审核列表展示。 \\

        \texttt{/videoInfo/getVideoPostDetail}
        & \texttt{videoId}
        & 读取投稿视频主信息和分 P 文件信息，用于审核详情页展示。 \\

        \texttt{/videoInfo/getAiAuditSummary}
        & \texttt{videoId}
        & 查询视频级 AI 审核任务摘要，包括审核建议、风险等级和任务状态等信息。 \\

        \texttt{/videoInfo/getAiAuditItems}
        & \texttt{videoId}
        & 查询分 P 级 AI 审核明细，用于展示风险片段和审核依据。 \\

        \texttt{/videoInfo/auditVideo}
        & \texttt{videoId}、\texttt{status}、\texttt{reason}
        & 提交人工审核结果。审核通过时进入发布同步逻辑，审核不通过时保存驳回原因。 \\
        \bottomrule
    \end{tabular}
\end{table}

管理员执行审核操作时，接口会接收视频编号、审核状态和驳回原因等参数。若审核状态为通过，主站服务会继续完成投稿态数据到正式发布数据的同步；若审核状态为不通过，系统只更新投稿态视频的状态和驳回原因，不会写入正式视频表。由于 AI 审核任务编排和发布同步流程在后续章节中单独说明，本节只保留后台审核入口和数据读取方式，不再重复展开完整审核链路。

通过上述实现，后台管理端在代码上承担的是管理入口和审核操作入口的作用。分类管理用于维护视频分区结构，内容管理用于查看投稿视频数据，视频审核管理用于读取投稿详情、AI 审核结果并提交人工审核结论。真正的视频状态更新、AI 审核结果保存和发布数据同步仍由主站业务服务负责处理。

\section{AI 审核任务编排与人机协同实现}

在 AI 审核这个环节当中，任务编排主要负责去衔接视频转码完成之后的自动初审以及后台的人工终审。鉴于前文已经对消息队列、审核服务以及后台管理端三者之间的总体协作关系进行了说明，所以本节不再去重复绘制完整的消息时序图，而是把重点放在说明 AI 审核任务如何去落库、审核结果如何去进行更新，以及人工审核通过之后如何去同步正式发布的数据。

在项目实现中，AI 审核相关数据并不直接写入投稿视频主表，而是拆分为视频级任务和分 P 级明细两类数据。\texttt{AiAuditTask} 用于保存一次视频级 AI 审核任务，包括任务状态、AI 建议、风险等级和审核摘要等信息；\texttt{AiAuditTaskItem} 用于保存每个分 P 文件的审核结果，包括分 P 建议、风险分值、风险标签和命中片段等信息。后台审核页面读取这些数据后，管理员再结合视频内容作出最终通过或驳回决定。

AI 审核与发布同步涉及的关键对象如图~\ref{fig:ai-audit-publish-class} 所示。该图只保留任务编排、审核结果和发布同步相关的核心类，不再展示查询条件对象和分页封装对象，以减少与前面章节的重复。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{images/ai_audit_publish_class.png}
    \caption{AI 审核任务与发布同步关键类图}
    \label{fig:ai-audit-publish-class}
\end{figure}

从状态角度看，视频投稿、AI 任务和人工审核之间存在较明确的先后关系。投稿视频先经过资源服务转码，转码完成后进入待审核状态；AI 审核任务用于生成初审建议；人工审核通过后，系统才会将投稿态数据同步到正式发布表。该状态变化如图~\ref{fig:ai-audit-state} 所示。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.86\textwidth]{images/ai_audit_state.png}
    \caption{AI 审核与发布状态流转图}
    \label{fig:ai-audit-state}
\end{figure}

\subsection{AI 审核任务创建与消息投递实现}

AI 审核任务创建发生在视频分 P 文件转码完成之后。资源服务完成文件处理后，会将转码结果回写给主站服务。主站服务根据同一视频下所有分 P 文件的转码状态进行判断：如果存在转码失败的分 P 文件，则视频状态更新为转码失败；如果所有分 P 文件均已转码成功，则视频进入待审核状态，并具备创建 AI 审核任务的条件。

任务创建时，系统先生成一条视频级任务记录，对应 \texttt{ai\_audit\_task} 表。该记录用于保存视频编号、请求编号、审核版本、任务状态、触发时间、模型信息和后续 AI 返回的视频级结论。随后，系统根据该视频下的分 P 文件生成多条任务明细记录，对应 \texttt{ai\_audit\_task\_item} 表。每条明细记录保存分 P 文件编号、文件序号、文件名、文件路径、时长和初始审核状态等信息。

AI 审核任务的主要数据对象如表~\ref{tab:ai-audit-data-object} 所示。

\begin{table}[!htbp]
    \centering
    \caption{AI 审核任务主要数据对象说明}
    \label{tab:ai-audit-data-object}
    \begin{tabular}{p{0.25\textwidth} p{0.28\textwidth} p{0.39\textwidth}}
        \toprule
        \textbf{数据对象} & \textbf{关键字段} & \textbf{实现作用} \\
        \midrule
        \texttt{AiAuditTask}
        & \texttt{videoId}、\texttt{requestId}、\texttt{taskStatus}、\texttt{aiDecision}、\texttt{aiRiskLevel}
        & 保存一次视频级 AI 审核任务，用于后台列表展示 AI 建议和整体风险摘要。 \\

        \texttt{AiAuditTaskItem}
        & \texttt{taskId}、\texttt{fileId}、\texttt{fileIndex}、\texttt{itemDecision}、\texttt{riskScore}
        & 保存每个分 P 文件的 AI 审核明细，用于审核详情页定位具体风险片段。 \\

        \texttt{VideoInfoPost}
        & \texttt{videoId}、\texttt{status}、\texttt{duration}
        & 保存投稿态视频主信息，转码完成后进入待审核状态。 \\

        \texttt{VideoInfoFilePost}
        & \texttt{fileId}、\texttt{videoId}、\texttt{filePath}、\texttt{transferResult}
        & 保存投稿态分 P 文件信息，为 AI 审核提供文件路径和分 P 信息。 \\
        \bottomrule
    \end{tabular}
\end{table}

消息投递时，系统不直接把视频文件写入消息队列，而是将审核任务编号、视频编号、分 P 文件编号和文件路径等信息封装为审核请求消息。AI 服务消费消息后，根据文件路径读取待审核视频，并在处理完成后返回结构化审核结果。通过“任务先落库，消息后投递”的方式，即使消息发送失败或 AI 服务处理异常，系统也能根据任务表保留任务状态和失败原因。

\subsection{审核结果接收与任务状态更新实现}

AI 服务完成审核后，会将视频级结果和分 P 级结果返回主站服务。主站服务接收结果时，首先根据请求编号或任务编号定位 \texttt{AiAuditTask}，再更新任务主表和任务明细表。视频级结果主要写入 AI 建议、风险等级、审核摘要、模型名称、模型版本和完成时间；分 P 级结果主要写入分 P 建议、风险分值、风险标签、命中片段和审核说明。

审核结果更新时需要注意任务状态的一致性。任务创建后通常处于待处理状态，消息投递或 AI 服务开始处理后进入处理中状态，AI 结果成功回写后更新为已完成状态。如果消息投递失败、AI 服务异常或结果解析失败，则任务应更新为失败状态，并记录错误信息。对于没有有效待审文件的情况，任务可以被标记为取消状态。

命中片段是 AI 审核结果中比较重要的部分。它通常以 JSON 形式保存到 \texttt{\seqsplit{hitSegmentsJson}} 字段中，用于记录风险出现的时间范围、风险类型、文本摘要、声音事件和模型判断理由等内容。后台审核详情页可以根据这些数据展示分 P 级风险证据，帮助管理员快速定位问题片段。

由于 AI 审核结果只承担初审提示作用，因此 AI 结果回写不会直接把 \texttt{\seqsplit{video\_info\_post.status}} 修改为审核通过或审核不通过。投稿视频仍然保持待审核状态，等待管理员在后台执行人工审核操作。这样可以避免模型误判直接影响视频是否公开发布。

\subsection{AI 初审与人工终审协同实现}

AI 初审与人工终审的协同关系主要体现在决策边界上。AI 服务负责根据视频内容生成审核建议、风险等级和命中片段，后台管理端负责展示这些结构化结果，管理员负责作出最终审核结论。也就是说，AI 结果用于缩小人工复核范围，但不直接替代人工审核。

后台审核列表可以展示视频基础信息、投稿状态、AI 建议、风险等级和审核摘要。管理员进入审核详情后，可以继续查看分 P 文件列表和 AI 审核明细。如果某个分 P 文件存在风险标签或命中片段，管理员可以根据片段时间范围、风险说明和播放资源进行复核。对于 AI 建议通过的视频，管理员仍然可以人工驳回；对于 AI 建议驳回或人工复核的视频，管理员也可以结合实际内容进行判断。

该协同方式能够保证系统审核流程具有一定的自动化能力，同时仍然保留人工终审的控制权。AI 审核负责提高风险发现效率，人工审核负责保证最终发布结果符合平台规则。对于本文项目规模而言，这种处理方式实现成本较低，也更适合毕业设计系统的可展示和可解释要求。

\subsection{审核通过与发布数据同步实现}

审核通过与发布数据同步由主站服务中的人工审核逻辑完成。管理员在后台提交审核结果后，系统首先校验视频是否仍处于待审核状态，避免已经处理过的视频被重复审核。校验通过后，系统更新 \texttt{video\_info\_post.status}。如果审核结果为驳回，则只保存投稿态审核状态，不会写入正式视频表。

如果审核结果为通过，系统会将投稿态数据同步到正式发布数据中。主站服务先将 \texttt{VideoInfoPost} 复制为 \texttt{VideoInfo}，写入或更新正式视频主表；随后删除该视频原有的正式分 P 文件记录，再将 \texttt{VideoInfoFilePost} 列表复制为 \texttt{VideoInfoFile} 并批量写入正式分 P 文件表。同步完成后，视频才会进入首页、搜索和播放页等公开视频访问流程。

审核通过时，系统还会将投稿态分 P 文件的更新标识重置为未更新状态，并清理 Redis 中等待删除的旧文件路径。如果该视频是首次发布，系统还会给投稿用户增加一定数量的奖励硬币。最后，系统调用 Elasticsearch 组件保存或更新视频索引，使审核通过的视频能够被关键词搜索命中。

审核不通过时，系统不会同步正式表，也不会更新搜索索引。创作者后续可以在创作中心看到稿件未通过状态，并根据审核意见修改后重新提交。通过这种投稿态表和正式发布表分离的方式，系统能够保证未审核通过的视频不会进入用户端公开访问链路。

\section{多模态 AI 审核服务实现}

多模态 AI 审核服务是 AI 初审的具体执行模块。该服务接收主站服务投递的视频审核请求后，按照分 P 文件逐个处理视频内容，并输出分 P 级和视频级审核结果。

AI 审核服务不会直接将完整视频输入多模态模型，而是先对视频进行预处理，将其拆分为音频、语音文本、声音事件和关键帧等结构化证据。随后，系统以时间窗口为单位构建审核片段，将同一时间段内的画面、语音文本、声音事件和投稿元数据组合为 \texttt{segment}，再调用多模态模型进行审核。

\subsection{AI 审核服务结构}

AI 审核服务可以按照处理职责划分为审核编排层、模型能力层、片段构建层和结果生成层。审核编排层负责控制完整审核流程，包括接收审核请求、初始化模型服务、调用视频处理方法、收集异常信息和返回最终结果。模型能力层负责封装具体模型调用，包括 VAD 语音活动检测、Whisper 语音转写、YAMNet 声音事件识别和 Qwen 多模态审核。片段构建层负责将不同模态的数据按照时间戳对齐，生成可供模型审核的 \texttt{segment}。结果生成层则负责将片段级结果归并为分 P 级和视频级审核建议。

\texttt{ModerationEngine} 属于审核流程入口和运行状态管理，\texttt{VideoModerationService} 为具体的视频分析和模型调用。前者负责加载服务对象、控制同一时间的审核执行、维护模型健康状态和处理运行异常；后者负责视频音频流检测、音频提取、语音识别、声音事件识别、关键帧抽取、片段构建和模型审核。通过这种划分，系统能够将流程控制和模型能力调用分离，降低单个类的复杂度。

AI 审核服务结构如图~\ref{fig:ai-service-class} 所示。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/ai_service_class.png}
    \caption{AI 审核服务结构图}
    \label{fig:ai-service-class}
\end{figure}

AI 审核服务启动时，需要先完成模型加载和运行环境检查。审核请求进入后，系统会为本次视频审核创建一次模型输入追踪会话。追踪会话用于保存片段输入、提示词、代表帧路径、模型原始响应和解析后的结果。该设计并不直接影响审核结论，但有助于后续调试和复核。当模型输出结果与人工判断存在差异时，开发人员可以根据追踪文件回看模型实际接收到的图片和文本，从而判断问题来自片段构建、提示词设计还是模型本身。

整体来看，AI 审核服务结构强调“编排流程”和“模型调用”的分离。审核编排层负责把控处理顺序，模型能力层负责完成具体分析，片段构建层负责组织多模态证据，结果生成层负责输出可落库、可展示和可复核的审核结果。这样的结构能够使 AI 审核流程更加清晰，也便于后续替换模型或扩展新的审核能力。

\subsection{视频预处理与音频提取实现}

视频预处理的目标是将原始视频文件转换为后续审核模型可以使用的中间数据。系统首先通过 FFmpeg 相关能力检测视频中是否存在音频流。如果视频存在音频流，则提取出 16kHz 的 WAV 音频文件，供后续 VAD、Whisper 和 YAMNet 使用；如果视频不存在音频流，则跳过音频分析流程，但仍然继续进行关键帧抽取和视觉审核，避免纯画面视频因为没有音频而被漏审。

音频提取完成后，系统会形成两条分析路径。第一条路径是语音文本分析，系统先通过 VAD 检测音频中的有效语音片段，再调用 Whisper 将这些语音片段转写为文本。该路径主要用于判断视频中“说了什么”。第二条路径是声音事件分析，系统通过 YAMNet 对音频进行环境声音和异常声音识别。该路径主要用于判断视频中“出现了什么声音”，例如枪声、爆炸声、尖叫声、警报声等。

视频预处理阶段还需要提取关键帧。关键帧是后续 Qwen 多模态审核的重要视觉输入。系统不适合将视频中的所有帧都送入模型，因为这样会造成较大的计算开销，也会产生大量重复画面。因此，系统按照一定的抽帧频率提取代表帧，并结合风险声音时间点补充附近画面。通过这种方式，可以在控制模型调用次数的同时，提高对短时风险画面的覆盖能力。

视频预处理与音频提取流程如图~\ref{fig:ai-video-preprocess-flow} 所示。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{images/ai_video_preprocess_flow.png}
    \caption{视频预处理与音频提取流程图}
    \label{fig:ai-video-preprocess-flow}
\end{figure}

视频预处理与音频提取过程如算法~\ref{alg:video-preprocess-impl} 所示。

\begin{algorithm}[!htbp]
    \caption{视频预处理与音频提取实现算法}
    \label{alg:video-preprocess-impl}
    \begin{algorithmic}[1]
        \Require 视频文件路径 $videoPath$
        \Ensure 语音文本集合 $S$，声音事件集合 $A$，关键帧集合 $F$，覆盖问题集合 $C$

        \State 初始化 $S \leftarrow \emptyset$，$A \leftarrow \emptyset$，$F \leftarrow \emptyset$，$C \leftarrow \emptyset$
        \State 检测视频文件 $videoPath$ 是否存在音频流

        \If{存在音频流}
            \State 使用 FFmpeg 从视频中提取 16kHz WAV 音频文件 $audioPath$
            \If{音频提取失败}
                \State 将音频提取异常写入 $C$
            \Else
                \State 使用 VAD 检测语音片段集合 $V$
                \State 使用 YAMNet 检测声音事件集合 $A$
                \If{$V$ 不为空}
                    \State 调用 Whisper 对语音片段集合 $V$ 进行转写，得到语音文本集合 $S$
                \Else
                    \State 保持 $S \leftarrow \emptyset$，后续由视觉关键帧和声音事件继续构建审核片段
                \EndIf
            \EndIf
        \Else
            \State 跳过音频分析流程，并记录视频无音频流
        \EndIf

        \State 从声音事件集合 $A$ 中提取风险声音时间点集合 $T$
        \State 根据固定抽帧频率和风险声音时间点集合 $T$ 提取关键帧集合 $F$
        \If{$F$ 为空}
            \State 将关键帧提取异常写入 $C$
        \EndIf

        \State 返回 $S$、$A$、$F$ 和 $C$
    \end{algorithmic}
\end{algorithm}

通过上述处理，系统将原始视频转换为带有时间戳的语音文本、声音事件和关键帧数据。后续审核流程不再围绕完整视频文件直接展开，而是围绕这些结构化证据构建片段。这样既能降低模型输入规模，也能使审核结果更容易和视频时间轴对应。

\subsection{语音文本、声音事件与关键帧审核实现}

完成视频预处理后，系统需要将语音文本、声音事件和关键帧组织为可供 Qwen 审核的片段。审核片段并不只依赖语音文本生成，而是由语音文本、风险声音和关键帧共同触发。如果视频中存在有效语音，系统会根据 Whisper 的转写结果创建文本-视觉片段；如果视频没有语音但存在风险声音，系统会根据 YAMNet 识别出的声音事件创建声音事件片段；如果视频既没有语音文本，也没有明显风险声音，系统仍然会基于抽取到的关键帧创建视觉补充片段。通过这种多源触发方式，系统可以避免无语音视频或低语音视频完全绕过审核流程。

在语音文本处理方面，Whisper 的转写结果会被保存为带开始时间和结束时间的文本片段。系统根据文本片段的时间范围创建审核窗口，并在窗口内查找对应的关键帧和声音事件。这样在后续模型判断时，语音文本不会被孤立处理，而是能够结合当时的视频画面和声音上下文共同分析。

在声音事件处理方面，YAMNet 的识别结果会作为片段上下文加入审核输入。对于普通声音事件，系统可以只将其作为辅助信息；对于枪声、爆炸、尖叫、警报等风险声音，系统会以声音事件时间点为中心创建审核窗口，并查找附近的关键帧。若附近存在可用关键帧，则将声音事件和画面一起送入 Qwen；若视觉证据不足，则在片段中记录覆盖问题，供后续综合风险结果生成时参考。

在视觉处理方面，系统不会将视频中的所有帧直接输入模型，而是围绕关键帧构建视觉审核窗口。关键帧可以来自固定间隔抽帧，也可以来自风险声音附近的补充抽帧。对于没有语音文本的视频，只要系统能够提取到关键帧，就可以构建 \texttt{visual-only segment}。该类片段主要用于覆盖纯画面、背景音乐、无声视频或 VAD 未检测到语音的情况，从而减少因缺少语音文本导致的漏审问题。

多模态审核片段构建过程如图~\ref{fig:ai-segment-build-flow} 所示。系统分别从语音文本、风险声音事件和未覆盖关键帧中生成不同类型的审核片段，再将投稿标题、简介和标签等元数据加入片段上下文，最后交由 Qwen 进行多模态审核。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{images/ai_segment_build_flow.png}
    \caption{多模态审核片段构建流程图}
    \label{fig:ai-segment-build-flow}
\end{figure}

多源触发的审核片段构建过程如算法~\ref{alg:multi-source-segment-build} 所示。该算法的核心思想是：优先根据语音文本创建文本-视觉片段，再根据未覆盖的风险声音创建声音事件片段，最后根据未覆盖的关键帧创建视觉补充片段。通过这种顺序处理，可以减少重复片段的生成，同时保证没有语音内容的视频仍然能够进入视觉审核流程。

\begin{algorithm}[!htbp]
    \caption{多源触发的审核片段构建算法}
    \label{alg:multi-source-segment-build}
    \begin{algorithmic}[1]
        \Require 语音文本集合 $T$，声音事件集合 $A$，关键帧集合 $F$，投稿元数据 $M$
        \Ensure 审核片段集合 $S$

        \State 初始化审核片段集合 $S \leftarrow \emptyset$
        \State 初始化已覆盖时间窗口集合 $W \leftarrow \emptyset$

        \For{每一个语音文本片段 $t \in T$}
            \State 以 $t.start$ 和 $t.end$ 创建审核窗口 $w_t$
            \State 匹配窗口内或附近的关键帧和声音事件
            \State 构建 \texttt{text-visual segment}，并加入 $S$
            \State 将 $w_t$ 加入 $W$
        \EndFor

        \For{每一个风险声音事件 $a \in A$}
            \If{$a.timestamp$ 未被 $W$ 覆盖}
                \State 以 $a.timestamp$ 为中心创建审核窗口 $w_a$
                \State 匹配附近关键帧
                \State 构建 \texttt{audio-event segment}，并加入 $S$
                \State 将 $w_a$ 加入 $W$
            \EndIf
        \EndFor

        \For{每一个关键帧 $f \in F$}
            \If{$f.timestamp$ 未被 $W$ 覆盖}
                \State 以 $f.timestamp$ 为中心创建视觉审核窗口 $w_f$
                \State 构建 \texttt{visual-only segment}，并加入 $S$
                \State 将 $w_f$ 加入 $W$
            \EndIf
        \EndFor

        \For{每一个审核片段 $s \in S$}
            \State 根据代表帧、文本内容、声音事件和投稿元数据构造 Qwen 输入
            \State 调用 Qwen 获取片段审核结果
            \State 将风险等级、风险类型、审核建议和判断理由写入片段 $s$
        \EndFor

        \State 按片段开始时间对 $S$ 排序
        \State \Return $S$
    \end{algorithmic}
\end{algorithm}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/ai_segment_audit_result.png}
    \caption{分 P 文件 AI 审核结果展示界面}
    \label{fig:ai-segment-audit-result}
\end{figure}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/ai_segment_detail_result.png}
    \caption{风险审核片段证据展示界面}
    \label{fig:ai-segment-detail-result}
\end{figure}

算法~\ref{alg:multi-source-segment-build} 对应的后台展示效果如图~\ref{fig:ai-segment-audit-result} 和图~\ref{fig:ai-segment-detail-result} 所示。图~\ref{fig:ai-segment-audit-result} 展示了后台审核详情页中分 P 文件的 AI 审核结果，页面在播放预览区域下方以时间轴方式标记不同审核片段，并在右侧展示 AI 状态、AI 建议、风险分数、片段数量和片段类型等信息。图~\ref{fig:ai-segment-detail-result} 展示了某个风险片段的具体证据，包括片段时间范围、风险类型、风险分数、模型判断说明、语音文本内容、风险音频标记以及代表关键帧画面。该界面说明系统已将语音文本、声音事件和关键帧按照时间窗口组织为可查看的审核片段，便于管理员根据片段证据进行人工复核。

构建完成后，每个审核片段都会保存开始时间、结束时间、片段类型、代表帧路径、语音文本摘要、声音事件摘要和模型审核结果。片段类型主要包括 \texttt{text-visual}、\texttt{audio-event} 和 \texttt{visual-only} 三类。后台管理员查看审核结果时，可以根据片段类型判断风险来源，从而区分该风险是由语音内容触发、声音事件触发，还是由画面关键帧触发。

整体来看，该实现方式将语音、声音和画面三类信息按照时间窗口进行组织，使 Qwen 审核输入不再只依赖某一种模态。对于有语音的视频，系统能够结合语音文本与画面进行判断；对于没有语音但存在风险声音的视频，系统能够围绕声音事件补充画面证据；对于无语音或低语音视频，系统仍然可以借助关键帧构建视觉审核片段，从而提高视频审核的覆盖范围。

\subsection{综合风险结果生成实现}

片段级审核完成后，系统需要将多个 \texttt{segment} 的审核结果汇总为分 P 级结果，再进一步汇总为视频级结果。分 P 级结果用于说明某个视频文件是否存在风险，视频级结果则用于后台审核列表展示和管理员最终审核参考。由于一个视频可能包含多个分 P 文件，因此系统不能只根据某一个片段结果直接决定整个视频的 AI 建议，而是需要按照层级逐步汇总。

综合风险结果生成采用高风险优先原则。若任一片段被判断为高风险，则该分 P 的 AI 建议应倾向于驳回；若不存在明确高风险，但存在中风险、模型判断不确定、缺少视觉证据或声音事件无法确认等情况，则建议人工复核；只有所有片段均未发现明显风险时，分 P 级结果才建议通过。该策略能够避免高风险片段被大量低风险片段稀释。

综合风险结果生成流程如图~\ref{fig:ai-risk-result-flow} 所示。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/ai_risk_result_flow.png}
    \caption{综合风险结果生成流程图}
    \label{fig:ai-risk-result-flow}
\end{figure}

在生成分 P 结果时，系统会遍历该分 P 下的所有审核片段，提取命中风险的片段，并记录片段时间范围、风险类型、风险等级、模型原因、代表帧路径和文本摘要。对于证据不足但仍存在风险可能的片段，系统也需要保留对应说明，避免后台管理员只能看到一个模糊的“复核”结论。分 P 结果应当同时包含整体建议和具体证据，便于管理员定位问题内容。

在生成视频级结果时，系统继续对所有分 P 结果进行归并。若任一分 P 建议驳回，则视频级 AI 建议为驳回；若不存在驳回但存在人工复核，则视频级 AI 建议为人工复核；只有所有分 P 均建议通过时，视频级 AI 建议才为通过。对于多分 P 视频，视频级结果还应当记录风险分 P 编号和风险片段数量，使后台列表能够快速展示风险来源。

最终，AI 服务将视频级结果和分 P 级结果封装为审核结果消息返回主站服务。主站服务接收到结果后，将其写入 AI 审核任务表和分 P 审核明细表，供后台管理端读取。后台管理员查看视频审核详情时，可以根据 AI 审核摘要了解整体风险，也可以根据分 P 明细和片段证据进一步定位具体内容。

通过这种从片段级到分 P 级、再到视频级的逐级汇总方式，系统能够同时满足自动审核和人工复核两方面需求。自动审核给出初步风险判断，分 P 明细保留模型判断依据，人工终审再结合平台规则和视频内容作出最终处理。这样既避免了单纯依赖模型结论带来的误判问题，也提高了管理员定位风险内容的效率。
